# 更新日志

> 格式：[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)

> **编写规则**：更新日志是**面向用户的变更摘要**，不是技术文档。
> - **每条一句话（≤50 字）**，只写"改了什么、为什么改、达到什么效果"，说明大致内容和逻辑即可；
> - **不写代码级细节**：不出现函数名、内部变量名、代码结构（如"某函数拆为 N 个方法"）、公式、具体数值；
> - **不写细枝末节**：参数格式变化、参数默认值变化等不写；
> - **零碎修改合并**：不重要/零碎的修改总结为一句"优化了xxx"，不写细节；
> - 同版本内相似内容，同类型内容合并为一条；
> - 需要代码级细节 → 引用 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md) 等技术文档；问题排查记录 → [TECH_DEBT.md](TECH_DEBT.md)。


## v2.6（2026-08-31）

### lsp模块
- **再次尝试修复重连后散热器跳到默认档（125/4500）**：把控制基准绑定到真正收数据的连接，命令不再写入失效连接，散热器即时响应新下发
- **修复 B7X 后台自动重连失效**：被拉起时未连接散热器，后台持续重连不再依赖易失的内存实例，改用持久化的上次设备直接发起连接，无需打开界面

### magisk模块
- **删除 Gear 档位模式**：仅保留 PID 连续无级调节，控制链路更简单直接
- **PID 重构为单累积器**：移除 PID 输入滤波、方差门控、死区、近区降敏、降温抑制等复杂补丁；改为比例/微分融合 + 动态目标；详见 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md)
- **优化cpu扫描逻辑**
- **配置文件/webui补了一些遗漏的参数**
- **优化webui参数描述**
- **加回并优化电池温度输入滤波**：仅在温度更新时按更新间隔动态平滑，长期不更新时恢复实时值，抑制累积偏差
- **优化重连逻辑**


## v2.5（2026-07-30）

### 跨模块
- **新 B6X app（B6 B6X 超频工具 V2）全功能支持**：修复蓝牙连接后一直"扫描中"
- **B7X 散热器支持**：模块扩展到双设备
- **双 app 存活仲裁**：两个 app 最多一个后台存活，防止后台竞争
- **状态协议**：状态文件区分连接的 app/设备，支持双设备仲裁
- **广播协议区分设备**：B7X 独立广播通道，避免与 B6X 冲突
- **连接反馈更及时**：连接/断开瞬间即写入状态文件

### lsp模块
- **修复散热器锁死**：重连后命令写入失效连接被丢弃，下发前校验连接并自动重连自愈。详见 [TECH_DEBT.md](TECH_DEBT.md)
- **锁死自愈增强**：命令堆积（下发协程停摆）时自动接管消费，重连无法恢复时也能自愈，避免散热器停在固定功率档
- **自动跳转**：蓝牙连接时直接进入参数修改界面
- **冷启动自动连接**：打开 app 自动连接散热器并进入设置界面
- **修复 app 概率无法修改数据**：重连后控制实例与界面不同步导致指令被丢弃，连接时同步到实际控制实例
- **修复散热器锁死自愈失效**：重连后设备不再回传时自动尝试唤醒，多次无效即停止自动重连并提示强制重启 App（散热器重启无效），避免无限重连刷屏
- **修复断连后控制状态残留**：断连时清理控制实例，B7X 重连后回传处理更可靠
- **状态文件写入更稳定**：原子写避免半写状态被读取
- **优化了代码结构**（行为等价）：命名与职责按功能统一
- **修复散热器控制失效**：状态文件写入失败时自动回退直接覆盖写

### magisk模块
- **PID 控制优化**：比例/微分/积分响应可独立调节——积分高基准防过冲、微分带记忆衰减、输出按预算分配防积分囤积
- **PID 输入滤波默认关闭**：预测过渡期间强制滤波防跳变
- **修复 PID 积分项从未触发**：温度稳定时静差无法消除
- **D 项近区降敏可配置**（`PID_KD_NEAR`）：微抖时抑制微分放大，制冷强度不因微抖掉档
- **参数合并为多值格式**（旧格式仍兼容）。详见 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md)
- **温度检测兼容文件时间戳**：温度稳定时积分仍能持续累积；时间戳不可靠时自动退回数值比较
- **CPU 补偿门控滞回**：条件消失后补偿平滑收敛不跳变
- **热端过温保护**：过热自动削减制冷上限（`HOT_DERATE` 可配置），下发前硬钳制防限速阻挡，回落自动恢复
- **风扇控制优化**：转速独立计算解决滞后/卡死、降速防抖仅降速生效、冷热端→RPM 平滑可配置（`RPM_SMOOTH_ALPHA`）、限速智能感知方向（高温禁降/低温禁升）
- **1s 采集 / 5s 控制分离**：降低传感器读取频率与竞争窗口
- **单进程双设备架构**：按实际设备选广播通道与上限
- **温度预测迁移到 Gear**：Gear 用预测温度提前调档，PID 改用滤波平滑输入
- **温度变化检测优化**：以值比较为主，文件时间戳降级为次级刷新信号（不可靠机型自动退化）。详见 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md)
- **sysfs 路径与缩放独立大类**：`SYSFS_ENABLED` 独立开关控制
- **自动拉起散热器 app**：无 app 存活时自动拉起上次使用的 app，进入界面后自动切后台（默认关闭）
- **WebUI 顶部面板可拖高**：曲线/日志面板高度可拖动调整，纵轴刻度自适应
- **修复 WebUI 改参数被下次修改覆盖**
- **优化了代码结构**（行为等价）：配置解析、主循环、限速、档位表、PID 初始化等逻辑合并去重，清理死代码
- **修复**：仅开调试日志时无法生效的问题
- **优化了预测逻辑**，提高介入速度
- **优化了参数命名**，按类别统一 B6X/B7X 命名方式
- **PID 输出端平滑自适应滤波**：按温度变化速率动态平滑，减少散热器频繁小幅调整。详见 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md)
- **修复 PID 微分项失效**：温度稳定时微分不衰减，修正后恢复记忆衰减与降温积分惩罚。详见 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md)
- **散热器锁死自动重启**：实际制冷停滞且未达目标连续多次下发时，自动强制重启散热器 app 恢复控制（次数可配置）
- **修复监控模式被误判锁死**：watchdog 仅在实际控制时生效，纯监控模式不再误重启散热器
- **修复极端配置导致崩溃**：PID 计算加入除零防护，异常配置不再使守护进程崩溃
- **修复 WebUI 自动拉起分组无法收起**：开关开启时点击组头无反应，现可正常折叠/展开
- **修复 WebUI 若干交互问题**：档位拖拽重排、图表渲染防御、参数保存原子写
- **配置文件分组按 WebUI 界面布局调整**：段标顺序与界面一致，便于对照
- **修复风扇转速被硬性限制**：移除 6000 上限，转速交由限速逻辑约束
- **优化了细节**：传感器重扫时机、档位偏移清理、热重载后紧急干预生效
- **修复自动连接偶尔失效**：提前创建 MAC 记录文件，避免冷启动/后台重连缺目标
- **散热器锁死自动重启默认关闭**：仅在开启自动拉起时生效（自动拉起为前置条件）
- **新增散热器无反应诊断脚本**：设备端一键排查守护进程与连接状态
- **WebUI 配置分区默认全部收起**：组头点击手动展开，不再随开关状态自动开合
- **WebUI 时间窗口与图例打包整体缩放**：超宽时等比缩小，内容不再被截断
- **WebUI 实时数据栏单行完整显示**：超宽自动缩小字号，数字定宽不抖动
- **WebUI 制冷强度轴显示范围优化**：从 1 开始，最低覆盖到制冷起始强度
- **WebUI 新增 UI 诊断日志**：输出界面适配参数，便于定位问题
- **优化了 WebUI 间距布局**：图例间距、坐标单位位置、顶部边距微调
- **WebUI 断联空白按真实断开时长显示**：短断联窄空白、长断联封顶，不再固定宽度
- **断联短暂不重置控制状态**：保留 PID 记忆，输出不跳变
- **档位表自动重排**：自定义风扇/制冷数量不一致时自动配对，缺侧档位不生效并提示
- **调试日志分区合并**：电池调档+紧急、下发+风扇各并一组，开关更简洁
- **WebUI 输入框自适应 + 顶部高度保底**：改参不被撑宽，键盘/小窗下曲线不被压扁
- **PID 降温抑制**：温度下降时放缓积分建立，压制无谓制冷尖峰、降低噪音，强度可调可关
- **WebUI 开关优化**：分组内 0/1 设置的输入框改为开关控件，操作更直观
- **输入滤波更可靠**：温度变化后滤波一段时间，长期不更新时改回实时值并平滑衔接，抑制累积偏差
- **基准附近降温防过冲**：接近目标温度时按降温/升温快慢微调制冷，降温快防过冲、升温快稳住制冷

### B8X 分析
- **B8X超频工具V2** Manifest 分析完成：爱加密 + 自定义 JNI 双重加固，蓝牙层用独立 SDK
- **未适配**：爱加密加固需运行时脱壳 + 架构不兼容现有钩子体系 + 用户决定暂不进行
- 详见 [完整修复历程 第8节](参考资料/完整修复历程.md#8-b8x-超频工具-v2-分析记录) 和 [反编译报告](参考资料/decompile/b8x/反编译报告.md)

---

## v2.4（2026-07-29）

### magisk模块
- **温度趋势预测**：根据历史温度变化提前预判走向，消除 PID 等待延迟，控制更跟手（PID 输入加入预测层，预测值参与计算，**可能**可以进一步压制温度波动）
- **电池电流读取统一封装**：内部统一单位换算，调用方直接用规范单位
- **紧急干预简化**：从 CPU+电流双信号源改为 CPU 单源，紧急等级从 4 级降到 3 级
- **反补与趋势豁免合并成一个配置项**：两个开关并成一组
- **移除**：PID 电流补偿、Gear 电流紧急、无用死代码清理

---

## v2.3（2026-07-09）

### 跨模块
- **状态文件协议扩展**：新增散热器全参数回传（运行模式、冷热端温度、实际转速/制冷、目标温度），LSP 写、tempctrl 读

### lsp模块
- **管理器显示推荐作用域**
- **散热器数据从正确位置捕获**：修正了抓取回传数据的钩子指向
- **后台自动重连**：断开后无需打开 app 自动重连，支持远程断连和主动断连两种场景

### magisk模块
- **PID 连续无级调节模式**：可替代档位式控制，制冷强度连续平滑变化
- **挡位模式自动算风扇转速**：按冷端强度 + 热端温度自动决定风扇快慢，挡位表风扇转速变为上限
- **PID 温度平稳时启用积分**：温度稳定时消除控制偏差，温度波动时冻结防过冲
- **温度变化才读取传感器**：温度文件没更新就不读，减少无效操作
- **制冷强度→风扇转速映射引擎**：冷端与热端信号综合决定风扇转速
- **限速逻辑统一下沉**：各模式共用同一套限速执行
- **限速随温差自动调整**：温差越大变化越快，保留 0.1°C 精度
- **热端风扇响应双向滞回**：降温不急着降、升温不低于上次值，防温度微动引起抖动
- **平滑计算方向取整**：上升向上取整、下降向下取整，消除"渐进永远到不了目标"的问题
- **重启不丢状态**：重启后恢复上次制冷强度，PID 模式也参与
- **日志清理行数可配置**：日志超限时删多少行可调（0=不清理）
- **删除所有含实际下发参数的日志**：只保留错误和跳转日志，减少日志噪音
- **风扇转速发送值取整到 50 倍数**
- **配置系统重构**：双层总开关替代分组开关，多值参数改连续格式，参数顺序不再影响解析
- **PID 输入增加 CPU 温差和电池电流作为额外热源参考**
- **编译优化**：二进制体积缩小约 80%
- **修复多处问题**：文档与实际不一致、极端配置崩溃、热重载/配置解析异常、电流映射与蓝牙命令竞争。详见 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md)

### 注意
- **直接冷端模式未进入发布版本**：在 v2.3 开发过程中删除

---

## v2.2（2026-07-03）

### magisk模块
- **电流-挡位映射 + 温度融合**：以电池电流为主要依据定档位，温度在此基础上微调（带冷却期），充电/放电分开调节，档位变化时温度偏移自动保留
- **各功能独立开关**：紧急干预、反补、趋势豁免等可单独开关，配置热重载即时生效
- **调试日志系统**：总开关 + 按模块分区开关，开启时自动放开日志量限制
- **配置预扫描**：先解析开关再加载参数，热重载更快
- **电流单位优化**：内部统一换算，放电负值不再被误判为传感器错误
- **充电/放电调节默认全开**
- **温度偏移不再设上限**：由档位上下限自然约束
- **电流映射冷却行为统一**：遵守同一冷却配置

---

## v2.1（2026-06-18）

### lsp模块
- **基础 bug 修复**

### magisk模块
- **档位表可配置化**：自定义档位数量与每档参数，自动扩展
- **紧急干预 4 级**：需 CPU + 电流双信号叠加触发
- **紧急进入/退出策略可选**：升档或降档两种模式
- **电池第三区间阈值**：更细的温度分区
- **紧急退出恢复期**：退出后阈值逐步恢复灵敏度，避免反复触发
- **反补独立冷却**：反补生效后冻结几轮，期间只累积不调整
- **速率限制参数可调**：各参数每周期最大变化量可配置
- **默认档位表改为全部固定功率模式**：避免风扇转速上限不强制导致噪音突增
- **决策与执行分离**：计算层只算目标值，执行层带限速逐步到达
- **逐档变动改为每周期限速**：每个参数单次循环最多变化限速量
- **CPU 温度扫描优化**：只保留温度最高的若干区域
- **电流紧急降级用平滑处理**
- **反补按平均每周期温差计算**：支持冷却期累积
- **修复**：风扇每周期转速限制失效、高温退出紧急时降档幅度超限、断开重连时温度突变屏蔽

---

## v2.0（2026-06-15）

### 跨模块
- **状态文件通信**：守护程序与模块通过状态文件交互（LSP 写、tempctrl 读）

### lsp模块
- **广播接收器**：接收守护程序指令，完整控制散热器
- **修复 runFetchLoop 空队列忙等**：解决 DefaultDispatcher 线程 100% CPU 占用

### magisk模块
- **C 智能温控守护程序**：电池/CPU 双温控决策，通过广播指令控制散热器
- **查表法档位系统**：1~12 级，带趋势豁免 + 峰值反补
- **双重检查存活检测**：心跳 + 蓝牙连接状态双保险
- **阈值运行时可配置**：配置文件热重载，无需重启
- **日志持久化**：连续写入文件，超限自动滚动
- **修复**：脚本数组越界导致最高档风扇转速异常

---

## v1.0（2026-06-08）

### lsp模块
- **修复 BLE 连接**：解决 Android 16 蓝牙连不上散热器的问题
- **第 1 层**：设备连接后扫描不停止 → 连接后自动停扫
- **第 2 层**：界面状态不更新 → 连接后手动刷新界面
- **第 3 层**：GATT 服务发现不触发 → 绕过 Android 16 权限检查暗坑
- **第 4 层**：智能温控界面闪烁 → 去掉多余的实例创建
