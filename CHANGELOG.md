# 更新日志

> 格式：[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)

> **编写规则**：更新日志是**面向用户的变更摘要**，不是技术文档。
> - 只写"改了什么、为什么改、达到什么效果"，说明大致内容和逻辑即可；
> - **不写代码级细节**：不出现函数名、内部变量名、代码结构（如"某函数拆为 N 个方法"）；
> - 配置键名与默认值属于用户可操作信息，可以保留；
> - 同版本内相似内容合并为一条；
> - 需要代码级细节 → 引用 [逻辑说明.md](magisk模块(智能温控)/逻辑说明.md) 等技术文档；问题排查记录 → [TECH_DEBT.md](TECH_DEBT.md)。


## v2.5（2026-07-30）

### 跨模块
- **新 B6X app（B6 B6X 超频工具 V2）全功能支持**：修复蓝牙连接后一直"扫描中"（LSP 钩子 + tempctrl 解析 BLE=1/2 配合）
- **B7X 散热器支持**：模块扩展到双设备（LSP 识别分流 + tempctrl 双设备仲裁配合）
- **双 app 存活仲裁**：两个 app 最多一个后台存活，防止两个 app 后台竞争（LSP 写连接者字段，tempctrl 按 BLE=1/2 裁决）；B7X 暂不参与
- **状态协议**：B6X 文件 BLE=1/2 区分连接的 app，B7X 文件 BLE=6/7 区分连接的设备（LSP 写、tempctrl 读）
- **广播协议区分设备**：B7X 独立广播通道，避免与 B6X 冲突（LSP 注册接收、tempctrl 按设备发送）
- **连接反馈更及时**：蓝牙连接/断开瞬间即写入状态文件（LSP 即时写、tempctrl 校准配合）

### lsp模块
- **自动跳转**：对于 B6X 的两个 app，在蓝牙连接时，直接进入参数修改界面（改挂基类生命周期钩子）
- **冷启动自动连接**：打开 app 自动尝试连接散热器并进入参数设置界面（等效自动点「开始设置」，不再需要手动触发）

### magisk模块
- **自动拉起散热器 app**：无散热器 app 存活时自动拉起上次使用的 app（`APP_LAUNCH_ENABLED` 开关，因会闪烁一下app界面所以默认关闭，刷入时可用音量±选择开启与否，后续也可在webui界面/配置文件修改）；启动后延迟 30s 再开始工作（等待系统/蓝牙就绪，避开开机初期拉起 app 闪烁）；拉起后进入参数设置界面即自动切到后台（几乎无感，带兜底超时）
- **代码简化（行为等价）**：守护进程与 WebUI 重构——重复逻辑合并提取、删除死代码与无效果占位；配置键名、日志文案、行为语义逐字保留
- **风扇转速独立计算（解决滞后/卡死）**：风扇目标不再跟在 PID/Gear 输出后面，改为每周期下发前用限速后的实际制冷强度 + 热面温度独立计算，制冷与风扇各自限速后统一下发。解决 PID 模式下风扇转速长期滞后甚至卡死在低位的问题（排查记录见 [TECH_DEBT.md](TECH_DEBT.md) 已解决区）
- **1s 采集 / 5s 控制分离**：传感器与散热器数据改为每秒采集一次并缓存，5s 控制逻辑直接读缓存，不再每周期重复读 sysfs/状态文件，降低读取频率与竞争窗口
- **调试日志开关修复**：仅开调试日志（PERF_ENABLED=0、DEBUG_ENABLED=1）时，调试日志现在能正常生效
- **单例双设备**：从单文件单设备改为单进程双设备仲裁架构，按实际设备选广播通道、切换上限
- **制冷强度限速智能方向感知**：电池 ≥37.5°C 禁止降低制冷强度、≤32.5°C 禁止升高，基础值 10→25
- **风扇降速防抖重做**：`FAN_RPM_CHANGE_THRESHOLD` 由双向防抖改为**仅风扇降低时生效**（目标低于当前且差值 ≤ 阈值才保持当前值，上升方向自由爬升，解决 PID 下"数周期风扇不变"），默认值 100→200；**距最低转速 < 阈值×1.5 时防抖失效**
- **冷/热端→RPM 平滑可配置**：新增 `RPM_SMOOTH_ALPHA`（默认 33%，1~99），冷端指数映射与热端线性映射**共用**；热端原无平滑，现补 EMA 平滑且**滞回基于平滑后的值**
- **PID 输入滤波默认关闭 + 预测过渡强制滤波**：`PID_INPUT_FILTER_ENABLED` 默认 0，非预测时输入直通原始值；预测进入/退出后自动临时开启滤波 6 周期防跳变，退出混合目标改为当前有效电池温度
- **优化预测逻辑，提高介入速度**
- **参数命名统一**：按类别统一 B6X/B7X 的命名方式（配置、日志、代码），并审计修正参数名
- **代码简化**：限速执行、档位表解析重建、PID 初始化等逻辑合并去重；清理废弃的兼容垫片与死代码
- **温度变化检测回退**：不再依赖温度文件的修改时间判断温度是否更新（部分机型时间戳更新不可靠，导致温度实际在变却被大量跳过）。
- **sysfs 路径与缩放独立大类**：`SYSFS_ENABLED` 开关独立控制路径与缩放配置的加载（默认关闭），不再跟随性能总开关；配置移到 profile.conf 最底部，自动拉起开关置于文件最末

### B8X 分析
- **B8X超频工具V2** Manifest 分析完成：爱加密 + 自定义 JNI 双重加固，蓝牙层改用独立 SDK（非 WaspWing SDK）
- **未适配**：爱加密加固需运行时脱壳 + 架构不兼容现有钩子体系 + 用户决定暂不进行
- 详见 [完整修复历程 第8节](参考资料/完整修复历程.md#8-b8x-超频工具-v2-分析记录) 和 [反编译报告](参考资料/decompile/b8x/反编译报告.md)

---

## v2.4（2026-07-29）

### magisk模块
- **温度趋势预测**：根据历史温度变化提前预判走向，消除 PID 等待延迟，控制更跟手（PID 输入加入预测层，预测值参与计算，**可能**可以进一步压制温度波动）
- **电池电流读取统一封装**：内部统一单位换算，调用方直接用规范单位
- **紧急干预简化**：从 CPU+电流双信号源改为 CPU 单源，紧急等级从 4 级降到 3 级
- **反补与趋势豁免合并成一个配置项**：两个开关并成一组
- **移除**：PID 电流补偿、Gear 电流紧急、无用死代码清理

---

## v2.3（2026-07-09）

### 跨模块
- **状态文件协议扩展**：新增散热器全参数回传（运行模式、冷热端温度、实际转速/制冷、目标温度），LSP 写、tempctrl 读

### lsp模块
- **管理器显示推荐作用域**
- **散热器数据从正确位置捕获**：修正了抓取回传数据的钩子指向
- **后台自动重连**：断开后无需打开 app 自动重连，支持远程断连和主动断连两种场景

### magisk模块
- **PID 连续无级调节模式**：可替代档位式控制，制冷强度连续平滑变化
- **挡位模式自动算风扇转速**：按冷端强度 + 热端温度自动决定风扇快慢，挡位表风扇转速变为上限
- **PID 温度平稳时启用积分**：温度稳定时消除控制偏差，温度波动时冻结防过冲
- **温度变化才读取传感器**：温度文件没更新就不读，减少无效操作
- **制冷强度→风扇转速映射引擎**：冷端与热端信号综合决定风扇转速
- **限速逻辑统一下沉**：各模式共用同一套限速执行
- **限速随温差自动调整**：温差越大变化越快，保留 0.1°C 精度
- **热端风扇响应双向滞回**：降温不急着降、升温不低于上次值，防温度微动引起抖动
- **平滑计算方向取整**：上升向上取整、下降向下取整，消除"渐进永远到不了目标"的问题
- **重启不丢状态**：重启后恢复上次制冷强度，PID 模式也参与
- **日志清理行数可配置**：日志超限时删多少行可调（0=不清理）
- **删除所有含实际下发参数的日志**：只保留错误和跳转日志，减少日志噪音
- **风扇转速发送值取整到 50 倍数**
- **配置系统重构**：双层总开关替代分组开关，多值参数改连续格式，参数顺序不再影响解析
- **PID 输入增加 CPU 温差和电池电流作为额外热源参考**
- **编译优化**：二进制体积缩小约 80%
- **修复**：文档参数名与实际代码不一致、极端配置下除零崩溃、热重载切换控制模式后 PID 未重新对齐、电流-挡位映射冷却期阻止偏移回落、配置解析吞掉 PID 参数、App 自修复与温控抢蓝牙命令

### 注意
- **直接冷端模式未进入发布版本**：在 v2.3 开发过程中删除

---

## v2.2（2026-07-03）

### magisk模块
- **电流-挡位映射 + 温度融合**：以电池电流为主要依据定档位，温度在此基础上微调（带冷却期），充电/放电分开调节，档位变化时温度偏移自动保留
- **各功能独立开关**：紧急干预、反补、趋势豁免等可单独开关，配置热重载即时生效
- **调试日志系统**：总开关 + 按模块分区开关，开启时自动放开日志量限制
- **配置预扫描**：先解析开关再加载参数，热重载更快
- **电流单位优化**：内部统一换算，放电负值不再被误判为传感器错误
- **充电/放电调节默认全开**
- **温度偏移不再设上限**：由档位上下限自然约束
- **电流映射冷却行为统一**：遵守同一冷却配置

---

## v2.1（2026-06-18）

### lsp模块
- **基础 bug 修复**

### magisk模块
- **档位表可配置化**：自定义档位数量与每档参数，自动扩展
- **紧急干预 4 级**：需 CPU + 电流双信号叠加触发
- **紧急进入/退出策略可选**：升档或降档两种模式
- **电池第三区间阈值**：更细的温度分区
- **紧急退出恢复期**：退出后阈值逐步恢复灵敏度，避免反复触发
- **反补独立冷却**：反补生效后冻结几轮，期间只累积不调整
- **速率限制参数可调**：各参数每周期最大变化量可配置
- **默认档位表改为全部固定功率模式**：避免风扇转速上限不强制导致噪音突增
- **决策与执行分离**：计算层只算目标值，执行层带限速逐步到达
- **逐档变动改为每周期限速**：每个参数单次循环最多变化限速量
- **CPU 温度扫描优化**：只保留温度最高的若干区域
- **电流紧急降级用平滑处理**
- **反补按平均每周期温差计算**：支持冷却期累积
- **修复**：风扇每周期转速限制失效、高温退出紧急时降档幅度超限、断开重连时温度突变屏蔽

---

## v2.0（2026-06-15）

### 跨模块
- **状态文件通信**：守护程序与模块通过状态文件交互（LSP 写、tempctrl 读）

### lsp模块
- **广播接收器**：接收守护程序指令，完整控制散热器
- **修复 runFetchLoop 空队列忙等**：解决 DefaultDispatcher 线程 100% CPU 占用

### magisk模块
- **C 智能温控守护程序**：电池/CPU 双温控决策，通过广播指令控制散热器
- **查表法档位系统**：1~12 级，带趋势豁免 + 峰值反补
- **双重检查存活检测**：心跳 + 蓝牙连接状态双保险
- **阈值运行时可配置**：配置文件热重载，无需重启
- **日志持久化**：连续写入文件，超限自动滚动
- **修复**：脚本数组越界导致最高档风扇转速异常

---

## v1.0（2026-06-08）

### lsp模块
- **修复 BLE 连接**：解决 Android 16 蓝牙连不上散热器的问题
- **第 1 层**：设备连接后扫描不停止 → 连接后自动停扫
- **第 2 层**：界面状态不更新 → 连接后手动刷新界面
- **第 3 层**：GATT 服务发现不触发 → 绕过 Android 16 权限检查暗坑
- **第 4 层**：智能温控界面闪烁 → 去掉多余的实例创建
