# 更新日志

## v2.2（未发布）

magisk模块：新增电流-挡位映射模式，以电池电流为挡位调整依据，覆盖常规电池温度调档。分充电/放电两套逻辑，各自有独立开关，可配置倍率、平滑系数、回退阈值和温度偏移修正。无冷却期，推荐档位低于阈值时自动回退基准模式。
新增独立开关：电流紧急（默认关闭）、CPU紧急、反补、趋势豁免 — 各功能模块可独立启用/禁用，配置文件热重载即生效。
新增调试日志系统：DEBUG_MODE总开关+7个分区开关（传感器/紧急/电池/执行/连接/配置/主循环），开启时自动关闭日志体积限制。
电流单位优化：原始µA读数/10000转为0.01A内部单位，放电负值不再被当作传感器错误误杀。
配置解析优化：开关类参数预扫描，关闭时跳过对应详细参数。
lsp模块：无更新。


## v2.1（2026-06-18）

magisk模块：挡位表加入配置文件成为可配置项。
默认挡位表改为全部使用固定模式，因为智能温控模式的风扇转速上限疑似非强制生效，切换时易导致噪音突增。
挡位变化执行时机改为立刻，不再等待下一次循环。
CPU温度扫描优化，不再每次扫描所有疑似可用值，改为只使用最高的20个值。
新增紧急4，紧急级别计算方式改为电池+cpu总共，而非单独最高值。
电流紧急降级判断改为使用平滑后的值。
6.21更新：反补改为使用平均每周期温度变化值，而非绝对变化数值，以应对电池温度刷新时间不稳定。接口路径加入可配置项，反补阈值加入可配置项。新增紧急进入退出时可选策略。新增电池第三区间阈值。略微修改配置默认数值。
6.25更新：限制每次5s循环风扇转速变化最大300以进一步压制风扇噪音突变。抬高豁免触发下限，并增加持续靠近基准值且速度较快时重置可用次数。限制了高温时退出紧急的降档幅度。屏蔽断开重连的温度突变。
6.27更新：每循环挡位/风扇转速变化限制值改为可调。修复风扇每周期转速限制失效。
6.28更新：删除逐档调节机制，改为限制每个参数单次循环的变化量。
6.29更新：退出紧急后临时放宽电池温度调节阈值以压制挡位波动。
6.30更新：修了一堆bug。
7.2更新：回退到了6.29版，因为6.30版因截断了负电流导致无法进入电流紧急，而我改2.2版改嗨了忘了存档bug修复版了。
lsp模块：无更新。6.30更新：修了一堆bug。

## v2.0（2026-06-15）

lsp模块：为magisk模块提供app控制接口；修复 DefaultDispatcher 线程死循环导致 100% CPU 占用。
magisk模块：使用lsp模块提供的接口结合手机电池/cpu温度进行更合理的散热器性能调整。
6.17更新：修复了脚本中数组越界导致的最高档风扇转速不正常。

## v1.0（2026-06-08）

本版本仅修复了飞智散热器开发者工具app在安卓16无法使用的问题，并未实现智能温控功能。
虽然提供了控制app的接口，但是没有进行测试，不保证可用。
