性能参数总开关


调试日志总开关


传感器读数

gear（合并电池+紧急）

执行下发（合并风扇）

连接状态

配置加载

主循环

PID

sysfs 路径与缩放开关

自动拉起


电池温度路径

电池温度缩放系数
原始值 ÷ 缩放系数 以结果精确至 0.1°C 为宜

电池电流路径

电池电流缩放系数
原始值 ÷ 缩放系数 以结果精确至 0.01A 为宜

CPU 温度 sysfs 模板
%d 会替换为目标 thermal_zone 编号

CPU 温度缩放系数
原始值 ÷ 缩放系数 以结果精确至 0.1°C 为宜

CPU thermal_zone 扫描范围
K60 推荐 30~40

日志文件路径

日志上限（字节）
超限时删除最早的超出部分，0=关闭日志

CPU zone 全量重扫间隔（秒）
按此间隔重新全量扫描，更新保留的最高温 zone 列表

风扇升速限制
基础值 / 每°额外提高值

风扇降速限制
每周期最大降速量 / 最小降低幅度

制冷强度限速
基础值 / 每°偏离额外变化可量 / 最小上升幅度

目标温度限速（°C）
智能温控模式目标温度每周期最大变化量


自动拉起散热器 app


锁死自动重启周期数
实际制冷停滞且未达目标连续 N 次后 kill 散热器 app 并重新拉起；0=关闭

温度未变强制处理周期
温度值连续未变达 N 周期时强制进入一次计算，防止温度文件卡死控制停摆

断联保留状态周期数
断联少于 N 个控制周期不重置 PID 状态；0=关闭

控制模式


基准温度（0.1°C）

制冷映射
起始强度：低于此值时线性外推下限；指数：n^exp（÷100，150=1.50）

热端线性映射范围
热端在此区间线性映射到风扇转速，超出取边界

冷/热端→RPM 平滑系数%
冷端与热端共用，值越大越跟随原始值，越小越平滑

风扇转速范围
最低转速 B6X最高转速 B7X最高转速

热端每°过温制冷上限削减值

KP 比例增益(×1000)
P 项线性斜率，300=偏差3°满幅

KI 增益
上升基础值 / 上升每°偏离提高值 / 下降基础值 / 下降每°偏离提高值

KI 记忆
降温累计积分扣除量 / 每周期巡检降低值 / KI积分上限

KI 门控
方差阈值 / 方差采样数 / 死区

KD 微分
上升倍率 / 下降倍率

KD 记忆
每周期衰减值 / KD上限基础值 / 每°偏离额外提高值

KD 近区降敏
近区阈值 / 近区倍率

PID 输入滤波
开关 / 自动关间隔阈值 / 自动开间隔阈值

滤波强度
更新间隔 / 滤波强度

CPU 补偿
滤波强度 / 补偿系数 / 偏移量

PID 输出自适应滤波
开关 / 下限 / 温差增益 / 偏差增益 / 下降倍率

制冷强度范围
下限 / B6X上限 / B7X上限

三区间阈值（0.1°C）
死区与 ±1/±2 档边界

预测权重
升温 / 降温 / 窗口不足倍率

预测窗口
缓冲区大小 / 最大计算窗口 / 最小计算窗口

预测上升限制
最大温升限制 / 最小启动周期 / 启动阈值

预测平滑系数(%)
预测温度 EMA 平滑，越大越跟随原始预测值

档位变动冷却周期
每次档位变动后冻结 N 周期 ×5 秒，紧急不受限

电流-挡位映射开关
0=仅普通电池温度，1=电流映射推荐挡位

电流映射倍率
推荐档位 = 滤波电流(0.01A) × 倍率 ÷ 100

电流滤波强度（%）
电流 EMA 平滑强度，越大越跟随原始值

电流映射最低档位
推荐档位低于此值时回退到基准（电池温度）模式

过冲反补/趋势豁免开关
=0 禁用对应功能

反补三级阈值
默认 2 3 4 = 0.2°C/0.3°C/0.4°C 每周期

反补冷却周期数
反补生效后冻结 N 周期

趋势豁免锚点复位阈值
以区间中值为偏移量计算复位阈值，越过则复位豁免计数器

CPU 温度紧急开关
=0 禁用

CPU 紧急阈值（0.1°C）
> 值1/2/3 → 进入紧急 1/2/3，< 值0/1/2 → 恢复到 0/1/2

CPU 温度滤波强度（%）
越大越跟原始值，越小越滤波

紧急干预模式
0=表查，1=步进

强制最低档位
各等级强制最低档位（1~4 级）

退出钳制偏移量
cap = 高一级最低档 + 此值，仅降档模式=0 生效

升/降档步数
模式=1 时生效

紧急退出电池阈值
低于基准+此值 → 全效退出，×2 → 半效退出

紧急退出恢复期倍率
退出紧急后调档阈值逐步恢复：×P1→×P2→×P3→1

恢复阶段周期数
每阶段持续 N 周期

自定义档位表开关
=1 使用下方 GEAR_N，=0 使用代码默认表

档位表自动风扇转速
=1 用冷端强度+热端温度双映射计算风扇转速，挡位表风扇转速变为截断上限；=0 直接使用配置风扇转速

断联判定阈值（秒）

断联空白最大宽度（秒）
