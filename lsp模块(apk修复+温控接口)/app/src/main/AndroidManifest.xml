<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="飞智b6x增强模块"
        android:icon="@android:drawable/ic_menu_compass"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!-- ========== Xposed / LSPosed 模块元数据 ========== -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedminversion"
            android:value="53" />
        <meta-data
            android:name="xposeddescription"
            android:value="修复B6X/B7X超频软件在安卓16的蓝牙问题，通过status文件为智能温控magisk模块提供控制接口" />

        <!-- LSPosed 推荐作用域：管理器 > 模块 > 推荐勾选的应用 -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <!-- 最小启动壳：显示构建信息与 root 状态；界面本体属线 D -->
        <activity
            android:name=".SetupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

    <!--
        本批次不新增任何权限：
        - SetupActivity 只读构建信息、执行 su，二者都不需要权限
        - RootShell 走 Runtime.exec，不需要权限
        后续批次按需再加（如通知、前台服务），加之前先确认用途。
    -->

</manifest>
