# 待办

> **归口（2026-09-22 固化）**：本文件只记**待做事项（动作）**——要做什么、真机上要看什么判据。**已知缺陷及其状态与根因**归 [`TECH_DEBT.md`](TECH_DEBT.md)；同一事项两边都有时，TECH_DEBT 留状态与根因，本文件只留待做动作并链接过去，不在此另写一份状态。
> **规则**：已实现的任务不保留叙述——只留一行「裁定结论 + 日期 + 链接（TECH_DEBT / CHANGELOG / 对齐报告）」；同一时间只保留「未实现」项，不保留供复查的草案。

## 待做

- **电池温度停滞判断（新想法，未做）** — 电池温度停滞后视情况结合电池电流/CPU温度等相关数据进行判断
- **曲线着色改造（延迟，仅记录，勿执行）** — 曲线**不加断点**；颜色变化用**渐变**（非硬分段）；颜色**按各序列自身特性决定**，**弃用现有固定温度/颜色映射表**。设计细节与场景 UI 借鉴（B 整档刻度 / A 面积填充已单独立项）分离。

---

# 待定：自适应 pid 速率（可行性分析已完成，定义未定）

- 想法1：对比制冷强度与温度的方差（经变换处理后），制冷大于温度则降低倍率，小于则提高，提高/降低两侧可独立开关，制冷为 1 和上限时的部分不计入；以达到根据温度波动动态调节累计速度，以平衡温度控制效果与制冷强度波动，或者侧重其中一方的目的。
- 想法2：根据制冷强度值动态降低倍率；以达到更高制冷强度的值更难到达，且变化更平滑的目的。

> **状态**：可行性分析已完成（只读，未动任何代码）。总结论：**两个想法都可行，但定义不足**，各有必须先拍板的未定项——不补足定义无法实现。

**「倍率」的口径**：指 `PID_KDP` 与 `PID_KI_RATE`。**想法1 是在现有动态 KI 机制上修改，想法2 是新增。**

## 现状基线（分析确证）

- **`scale_up`/`scale_down` 只乘 KI 速率，完全不碰 KDP**（`pid_compute` 里 `pid_kdp` 用 `pid_kdp_coef` 原值）。所以"倍率作用于 KDP"是**新增作用点**，要改 `pid_kdp` 计算式，不是在现有基础上改。
- **时间语义不同**：`pid_kdp` 是每周期重算的瞬时量、不累积（只在温度变化时更新）；`pid_ki` 是累积量、不乘 dt（单位是"每重算周期"）。乘 KDP 只改本周期幅度，乘 KI 会改整个积分历史。
- **现有窗口只存原始电池温度一条序列**（0.1°C 码），且只在"温度窗口变化"时推入；**制冷序列从未出现过**。想法1 要新增第二条序列。
- **制冷是 PID 的输出**，从 `pid_out` 到下发隔 5 道加工：目标映射 → 限速 → 风扇独立计算 → 过温钳制 → 死区去重。有效上限 `active_cold_eff_max` **每周期随 `hot_derate` 重算**。

## 想法1 结论：可行，但定义不足

**扩展点现成**：升降两侧独立作用已是现状（`g_up`/`g_down` + `scale_up`/`scale_down` 按 `integrand` 符号分乘），是扩展不是冲突；还可顺带补上现有机制**没有总开关**的缺口。

**三处硬伤**：

1. **量纲** — 温度方差单位"码²"（默认锚点 T1 = 4.70 码² ≈ 峰峰值 0.9 ℃），制冷方差单位"制冷²"（1~190 整数，典型量级数百至数千）。直接比大小几乎**恒有"制冷 > 温度"** → 判据退化为恒真 → **永远降倍率**，与"动态调节"的意图相反。这不是调参问题，是会让功能失效的错误。原文"经变换处理后"未定义，**最阻塞的一项**。
2. **自激环** — 用控制器**输出的抖动**调**自己的增益**，构成最短闭环。现有动态 KI 用**输入侧**（温度）抖动，中间隔着被控对象，稳定性本质更好。振荡表现为中低频极限环，周期与窗口 N×5s（默认 180 s）同量级，且易被积分预算钳制与制冷限速掩盖成"偶发过冲"，不好定位。
3. **贴边排除可能把机制吃空** — 稳态时输出常逼近上限（高制冷）或下限（低负载），排除饱和区后样本可能长期少于门槛 6（窗口 N 默认 36）→ 机制整体退化为"不干预"。判定必须用**每周期重算的 `active_cold_eff_max`**（含 `hot_derate` 实时削减），不能用配置上限或 194/190 字面量。

**取样三条路**：目标制冷 `pid_align_cold`（闭环最短、自激最硬）／C 端实际 `actual_cold`（被限速削峰，系统性低估真实抖动）／设备回传 `COLD_REAL`（物理最真，但有已知不可信窗口，需复用 `report_ok` 过滤 −1 与占位值）。

## 想法2 结论：可行，但要先裁定一个方向冲突

- **与 `COLD_RPM_MAP` 不重复** — 一个调风扇转速（映射层）、一个调 PID 增益（控制层），可并存。但两者目的表述文字上很像，定义里要写明分工。
- **与 `RATE_LIMIT_COLD` 方向正相反** — 现在升速步长 = `基础值 + (电池温度 − 基准) × 倍率/10`，即**越热升得越快**；想法2 要让高制冷难到达。两级串联叠加后可能表现成"温度压不住、制冷又爬不上来"。
- **与 `HOT_DERATE` 有分母耦合** — 若用 `active_cold_eff_max` 做归一化分母，过温削减期分母变小 → 归一化值更高 → 降得更狠，等于过温保护期二次压制。
- **化解路径** — 若"高制冷"明确限定在**贴近饱和区**那段（已满负荷，再快无意义、平缓反而减少设备侧过冲），则与现有设计不冲突且与"饱和区排除"自洽。**拐点定义是关键裁定点。**

## 必须拍板的未定项

### 想法1（9 项）

1. 制冷序列取哪个量（目标 `pid_align_cold` / C 端实际 `actual_cold` / 设备回传 `COLD_REAL`）？
2. **「经变换处理后」的具体公式**：归一化分母取什么（θ / 量程 / `active_cold_eff_max` / 固定 194？），是否叠加现有 `pow_k` 变换？**最阻塞的一项。**
3. 两侧开关的语义：关掉是"完全恢复现状（含关掉现有温度侧削 KI）"还是"只关新增制冷项、现有温度项始终生效"？
4. 调制下界 `floor` 取多少？与现有 `PID_KI_DYN_GATE` 第二值共用还是新增？
5. 与现有动态 KI 的关系：串联相乘 / 取 min / 分工（如制冷项乘 KDP、温度项乘 KI）？
6. 是否作用于 KDP？（现状只作用于 KI；作用于 KDP 是新增作用点）
7. 是否新增独立总开关（现有动态 KI 无法关闭，只能把 T1 调极大使其不触发）？
8. 「在 1 或上限」的判定细则：用运行时 `active_cold_eff_max` 还是配置上限？边界是否含等号？留多少裕量？
9. 样本不足时的降级行为：沿用现有 `count_low`（不干预、保留旧 scale）还是别的策略？

### 想法2（8 项）

1. 输入量（同想法1 第 1 条）。
2. 归一化分母（`active_cold_eff_max` 会因 `hot_derate` 突变 / `active_pid_cold_max` / 固定 194 / `PID_COLD_RANGE` 第二值）？
3. **曲线形状与拐点**：线性 / 指数 / 分档？从哪个制冷值开始降？拐点是否可配？
4. 调制下界 `floor`（同想法1 第 4 条）。
5. 与现有动态 KI 的合成方式（同想法1 第 5 条）。
6. 是否作用于 KDP（同想法1 第 6 条）。
7. **与 `RATE_LIMIT_COLD` 升速侧的方向冲突如何裁定**：是否限定"仅在接近 `active_cold_eff_max` 的一段生效"？
8. `hot_derate > 0` 期间想法2 是否暂停，防二次压制？

## 实现落点与次序

- **落点**：`ki_dyn_push` / `ki_dyn_eval` / `ki_dyn_ema` / `ki_dyn_reset` / `ki_dyn_log` 五函数 + `pid_compute` + `pid_cycle` + `parse_pid_cfg`/`ki_dyn_cfg` + `pid_reset_core`。配置面只需在 `参数定义/params.def.json` 加一行，其余产物自动生成（R1 已落地）。
- **次序**：R1 已落地，原先「先冻结 def 格式」的前置**已解除**——新键现在只是在 `params.def.json` 里加一行，`profile.conf`／`逻辑说明.md` 参数表／C 端边界宏与键表由 `gen_params.py` 生成，`check_params.py` 在 CI 里对账。
- 与 R2/R3/R4/R7 均无关（都已落地），都不用等。**唯一注意点**：若要把新量（制冷方差、当前 scale）画进曲线，会改曲线数据文件的 8 列行格式——写端 `WEBUI_ROW_FMT`／`WEBUI_DATA_COLS` 已由 CI 两条断言把守（转换符个数、实参个数），读端 `ui/ChartDataset.java` 的 `COL_*` 列常量与第 8 列去向说明也要同步。
- 新增状态必须挂进 `pid_reset_core`（现有 `ki_dyn_reset()` 就挂在它末尾），否则切模式/重连后会带着旧窗口与旧削减档继续跑。
- 若新增配置键，`params.def.json` 的 min/max 与 C 端 clamp 的一致性、以及 `逻辑说明.md` 参数表的同步，均已由生成器与审计覆盖，无需手工核对。

---

# 未完成：跨文件重复信息重构（余 1 项）

> R3 / R7 已于 2026-09-20 落地（C 端 status 字段常量表 `STF_*` + `status_field_value()`；曲线读端列常量与第 8 列去向说明）。落地明细见 [参数定义/对齐报告.md](参数定义/对齐报告.md) 与 [CHANGELOG.md](CHANGELOG.md)。

| # | 重复的信息 | 现状 | 收敛到 | 备注 |
|---|---|---|---|---|
| **R16** | 已删除机制的「墓碑」条目 | `逻辑说明.md` 内 15 处声明 Gear / `PID_OUT_FILTER` / `BOOT_AT` / `CTRL_MODE` / `LOG_TRIM_LINES` / `PID_GAIN` / `webroot` 等已删 | 无（保留） | 低优先；任一机制被重新引入需同时改这些句子 |

---

# 未完成：命名残留

- **`CFG_WEBUI_KEY_COUNT` 名字已过时** — 五个界面专用键已由 `WEBUI_*` 改名为 `UI_*`，但生成头里这个"仅界面读取键的个数"宏仍叫 `WEBUI_`。它只是计数、无键名语义，改名要动 `gen_params.py` 的写出处；属遗留命名，不影响功能。
- **`参数定义/对齐报告.md` 与 `.claude/`、`参考资料/路线记录/` 下的历史笔记里仍有 `WEBUI_*` 键名** — 属**当时事实的记录**，不改写历史。

---

# 已知缺陷与有意取舍（状态已归口 TECH_DEBT.md）

> 下列 4 项均为**已裁定不做或属设计约束**、无待做动作，其状态与根因按归口规则归 [`TECH_DEBT.md`](TECH_DEBT.md) §8，本节不再各写一份：
> `profile.conf.tmp` 卸载残留、`tempctrl_last_dev` 卸载时无法清理、「构建成功」≠「编译过且无告警」、配置键改名的兼容性。

---

# 需真机确认（2026-09-21 第四轮改动 / 补丁轮一~三）

> **归口**：本节只写**要验什么、判据是什么**（待做动作）。改动是否已修、根因与缺陷状态见 [`TECH_DEBT.md`](TECH_DEBT.md) §3 / §4——本节不另写一份状态。
> 本机无 Android 运行环境，CI 只证明「能编译」。以下只有源码级依据，从未在设备上跑过。
> **提交状态（2026-09-21）**：第 1~41 条已 commit `f9f55db0` 并 push；第 42 条起（补丁轮一~三）**未 commit、未 push，CI 未跑**。
> 被取代的条目保留在原处、就地标注取代关系。条目编号贯穿本节全文；后补观察点以 `18b` 形式追加，不重排既有编号（改早期条目号会牵动全节交叉引用）。

## 曲线区（W1）

- **1. 图例内部两行间距、「图例→画布」间距是否已与「图例→时间选择栏」三处视觉等距** — 通过 = 三处肉眼看齐。不达预期的头号原因是图例项 `minHeight` 24dp 只是下限：CheckBox 自然高若 >24dp 就压不动（此时只能改图例项高度本身，不再调间距）。其中「图例内部两行间距」已由第 45 条取代（项高 20dp + 行距 0dp，实得 2dp）；「三处等距」的判据仍有效。
- **2. 图例项 20dp / 档位按钮 21dp 的可点面积是否误触明显** — 两者都低于 Material 建议的 48dp 触控目标；通过 = 多次点按不出现「想点 A 中 B」。误触明显则抬高对应 `dimen`（会连带改回间距，与第 1 条联调）。图例项高度已由第 45 条改为 20dp，可点面积以该条为准（档位按钮 21dp 未变）。
- **3. 档位按钮 21dp 高（12sp 行高约 17dp + 上下各 2dp）文字是否被裁** — 通过 = 「60s / 5m / …」上下不出框、不被截断。
- **4. 最下刻度数字底边 → 拖柄横条顶边只剩 1dp（改动前 9dp）** — 通过 = 观感不相碰；挤的话最小回调是把 `ChartView.PAD_B_DP` 从 4 回到 5~6。
- **5. 最低那条系列标注可能贴到拖柄横条 0dp** — 触发形态是「数据有断点且最后有效采样点在图中部」；通过 = 该形态下标注仍可读，仍贴则给最低标注加最小下让位。
- **6. 绘图区顶部 `PAD_T_DP` 17 是下限**（纵轴单位标题 baseline = `fPadT − 7dp`，10sp ascent ≈ 9.3dp）— 通过 = 单位标题没被裁到画布外、不与顶边相碰。

## 外壳（W2）

- **7. 顶栏改为上 4dp / 下 8dp（不再等距）** — 通过 = 标题不贴状态栏、不被裁。注意状态栏那段是系统 inset，不参与本次调整。
- **8. 底栏 `bottom_nav_pad_bottom` 11 → 3dp（文字下移 8dp）** — 通过 = 文字与图标留出约 2dp 间隙、且不贴到栏底。系统字体放大时可能重新相挤，下一个旋钮是该值再降或抬 `bottom_nav_height`。
- **9. 键盘弹起时底栏内容区被压成 0（已取代，见第 47 条）** — 原「真机看底栏是否变空或塌掉」的判据作废；现验法见第 47 条（反复拉起/收起键盘 5 次以上，底栏高度不再变化）。根因见 [`TECH_DEBT.md`](TECH_DEBT.md) §4.4。

## 状态页（W3）

- **10. 切回状态页是否零视觉变化** — 状态区**原文不变**、不跳、不闪；进度条会出现（常占位 `INVISIBLE`，不引起任何高度变化），它是刷新唯一的反馈；高度变化只来自手动路径的文本补间（那正是要的效果）。
- **11. 静默刷新 30s 节流** — 节流期间设备状态真的变了（如 daemon 被外部杀掉）时状态区最多滞后 30s，通过 = 可接受；同时验手动「刷新状态」不受节流。
- **12. 首次进页的探测（`firstProbe`）也是静默的**（不碰状态区文本、不写操作记录、不做动画），但**进度条同样显示**——它走的是同一条常占位通道，故不再是「页内只有静止的读取中…」：全新安装且 root 授权框未作答时，是「读取中…」+ 进度条转动约 8s+。
- **13. 手动刷新动画**（文字淡入淡出 150ms + `AutoTransition` 补间）— 通过 = 不觉得「补间慢半拍」；觉得慢就改成 `ChangeBounds`（一行之差）。补间**范围**另见第 50②：范围是整页根，会把日志卡一起长高。
- **14. 操作记录卡（已取代，见第 50 条）** — 原「固定 200dp 高 + 内部滚动」已改回「自适应高、无内部滚动」（与诊断信息同款）；观感与滚动手感按第 50 条验。
- **15. 拉起daemon（原「拉起守护进程」）** — 一次点击约 **30~35s 温控空窗**（C 端 `BOOT_START_DELAY_SEC=30`）；验「新 pid ≠ 旧 pid」。旧实例 30s 未退出会放弃启动，此时按钮必须给出明确失败文案、不许静默失败；杀完起不来要等看门狗最长 300s。
- **16. 哈希不一致弹窗** — 判据是「已部署 且 二进制哈希不一致」，去重键 `bin_hash_prompted_md5`（记住已提示过的 expected md5）→ 每个 APK 版本最多弹一次。通过 = 该弹时弹、同版本不重复弹。已知边界：设备侧二进制被改坏而 APK 侧 expected 未变时，首次提示后不再弹。脚本哈希不一致仍是静默自动重推（不弹窗）。
- **17. 三个按钮行重排后的观感** — 行1 `[一键部署][拉起daemon]`、行2 `[su诊断][刷新状态]`（都用 su 诊断的 TextButton 样式）、行3 `[卸载部署]`；通过 = 不错行、不挤压（是否换行由窄屏决定）。
- **18. 状态页根改成 `PageScrollView` 后的页面纵向滚动手感**（改这个是为了让「操作记录」内部能收到纵向拖动）。
- **18b. 进度条处于 `INVISIBLE`（空闲态）时是否仍在跑 indeterminate 动画、白占 CPU** — 本机无法验证（material 的 `BaseProgressIndicator` 通常不可见即不绘制）。若真机确认不可见时仍占 CPU，改法是改用 material 自带的 `hide()` / `show()`（同样不改布局，常占位不变）。

## 参数区（W4）

- **19. 数值框不再冲出窗口右边界；path 框吃掉本行剩余宽。**
- **20. 长说明不再把字段块撑高** — 单字段键的说明行已改挂行级（宽 304dp），多字段键的说明仍保持字段宽；通过 = 长说明键与短说明键的字段块等高。
- **21. 「说明比内容宽时两侧贴圆角、说明窄时居中」是否成立** — 由框宽计算保证，`gravity=center` 未改。
- **22. path 框右侧内容可见（新增 `scrollHorizontally`）+ 失焦后滚动位置在本次启动内保持**（仍不落盘）。
- **23. 同键多字段的并排行为会变** — 可用宽不足时从最宽的框开始削（下限 72dp），被削的框其 `hint` 可能被省略号截断；极端情况下（2 个 72dp 框都放不下）交给流式容器换行成 2+1 行，不会退化成「每字段独占一行」。通过 = 实际观感可接受。
- **24. 正文竖直居中（已取代，见第 44 条）** — 原「按『说明下沿 → 框底』区间取 12/4dp」已改为 8/4dp（正文上移 2dp，与参数名盒仍不同高）；「与参数名盒严格同高」的取舍与备选见第 44 条。
- **25. 控制区宽变化（旋转 / 分屏 / 首帧）时不再打断正在输入的光标位置**（新增光标保存/还原）— 通过 = 旋转后光标仍在原字符位。

## 日志页（W5）

- **26. 搜索框与命中计数已移进「日志区」卡内、固定在日志列表上方** — 左右各 8dp 与日志行左边缘对齐；占掉列表高度约 12+49=61dp（命中计数可见时再减一行）。通过 = 对齐无偏差、列表没被压得过短。

## 设置页（W6）

- **27. 新增「参数重置栏」5 个按钮（`[0]日志&调试` … `[4]界面`，去段标显示）** — 点击弹 appcompat 确认框 → 确认后整组回出厂值（`factoryValue`）、一次性 `setAll` 落盘、重置前先冲刷待写队列。通过 = 确认框措辞得当、取消无副作用、确认后值真变。
- **28. 注意**：设置页只显示 `[4] 界面` 组，点 `[0]`~`[3]` 会真的把那 4 组恢复出厂值但**本页看不到变化**（只弹 Snackbar）。确认这是你想要的范围。
- **29. 重置后本页即时重绘；daemon 下一轮轮询热重载；`UI_BACK_HIDE` 经 daemon 转写 uiprefs 最多 1 周期生效。**
- **30. 已知竞态**：重置写盘是异步的，若在写盘完成的毫秒级窗口内立刻返回配置页，配置页那次重读可能读到旧值（与既有「改即存」队列的时序特性一致）。
- **31. 死参数 `UI_AUTOSAVE` 已删除（键数 53→52）** — 老设备 `profile.conf` 里会留一行孤儿键，界面诊断会显示「配置含未定义键」，**永不自动清理**（既有行为）。

## 卸载自清（W7）

- **32. 卸载 APK 后 daemon 应在约 5~10s 内自清并自停（最坏约 65s）** — 清理范围含 `/data/local/tmp` 下的二进制 / status 双文件 / uiprefs / 服务日志 / 旧残留、`/data/adb/service.d` 与 `/data/adb/ksu/service.d` 下的脚本、`/cache/tempctrl.log`；**不删** `profile.conf`（系统卸载会连带删私有目录）。
- **33. 真机看 `/data/adb/service.d`（及 `ksu/service.d`）下的脚本是否真被清掉** — 脚本兜底「删自己」是主路径；SELinux 域是否允许 daemon `unlink` `/data/adb/**` 属未验证项，状态与根因见 [`TECH_DEBT.md`](TECH_DEBT.md) §4.1。
- **34. 真机确认 `/data/local/tmp/tempctrl_b6x/b7x.status` 何时才彻底消失**（须重启飞智 app 或重启设备）— 状态见 [`TECH_DEBT.md`](TECH_DEBT.md) §4.2。
- **35. 真机确认「卸载与重装间隔 >10s 触发的自毁」不会误判**（设计意图：重装后必须重新「一键部署」）— 防抖措施、最高代价项与未覆盖面见 [`TECH_DEBT.md`](TECH_DEBT.md) §4.3。
- **36. 未覆盖场景** — 只停用 LSPosed 模块（不卸载 APK）时**不会**自清（按需求字面「卸载」实现）。
- **37. 脚本兜底路径最坏延迟约 300s（看门狗周期）。**

## 返回隐藏后台（W2b）

- **38. 钩子挂载点由 `android.app.Activity` 改为 `androidx.activity.ComponentActivity`** — 宿主 Activity 全是 AppCompatActivity，原挂法**一次都没触发**，这是「开关无效」的根因；原挂法保留为非 androidx 页面兜底。与第 48 条一并验。
- **39. 关闭开关后按返回是否恢复系统默认退出（已取代，见第 48 条）** — 语义加上了「从最近任务隐藏」、判据放宽到宿主包内任意页面，并给出 logcat 期望行；注意本条「只出现一行」的口径已不适用（现在期望两行：`app 已切后台（返回键）` + `已从最近任务隐藏（taskId=n）`）。
- **40. B7X（farsef，targetSdk=29、混淆产物）未验证是否同样走 androidx** — 若 logcat 显示「无 androidx.activity.ComponentActivity」则是走了兜底钩子，行为与原版一致（不会更差）。
- **41. 次因（本轮未改）** — `readBackHideEnabled()` 读 `/data/local/tmp/tempctrl_uiprefs`，普通 app 进程对该目录的可读性未知；读失败会回退「默认开启」，不会造成「开关无效」。若真机上关掉开关仍隐藏后台，第一个要查这里（`ls -lZ` + 进程 SELinux 上下文）。状态与同源项见 [`TECH_DEBT.md`](TECH_DEBT.md) §3.3。

## 间距微调（第四轮补丁轮）

- **42. 状态页按钮行间距与进度条上间距由 7/8dp 压到 5dp**（两行按钮的间隔已合并为同一个 `dimen`）— 通过 = 按钮行之间、进度条上方仍能一眼分出成组；过密就把该 `dimen` 调回 6~7dp。
- **43. 日志搜索框上方间距 12→8dp** — 通过 = 搜索框与卡头不挤；过挤回调该 `dimen`。
- **44. 参数输入框竖直内边距 12/4dp → 8/4dp**（正文整体上移 2dp）— 现在比「说明下沿 → 框底」的中点**高** 2dp、比参数名盒**低** 2dp（**与参数名盒仍未严格同高**）；通过 = 比之前顺眼。要与参数名盒严格同高需把底部 4dp 改成 8dp（代价：正文离说明只剩约 7dp）。
- **45. 曲线图例：项高 20dp、行距 0dp → 两行方框之间可见空白 2dp（原 14dp）** — 真机重点看两条：① **深色主题下勾选框的「挖空对勾」**（透出卡片底色，对比度略低于 material 原来的 onPrimary 对勾）是否可接受；② **20dp 纵向触控目标 + 相邻项触控矩形相接无死区** → 落在边界几 dp 内的点会切到相邻系列（横向另有 48dp 宽兜底），误触是否明显。①太淡就给对勾另设颜色；②明显则加大项高（会连带改回行距）。
- **46. 曲线高度调节横条已修回水平居中（`top|center_horizontal`）** — 连同「横条贴画布下沿、最下刻度数字离横条顶仅 1dp」（第 4 条）一起看：通过 = 横条水平居中且不与刻度数字相碰。

## 行为复核（第四轮补丁轮）

- **47. 底栏被输入法逐次压低（已修）** — 验法 = **反复拉起/收起键盘 5 次以上，底栏高度不再变化**；三键导航机型另看底栏是否仍铺到系统条之下。取代第 9 条；根因与状态见 [`TECH_DEBT.md`](TECH_DEBT.md) §4.4。
- **48. 「返回隐藏后台」重做** — 语义变为「收进后台（保活、BLE 不断）**且**从最近任务隐藏」；判据放宽为「开关开 + 非 finishing + 宿主包内任意页面（排除透明转发页 `UtilsTransActivity*` / `HolderActivity`）」，不再硬要求任务根。验法（看 logcat 期望行）：设置界面按一次返回 → 应出现 `app 已切后台（返回键）` + `已从最近任务隐藏（taskId=n）`，app 停后台、下拉最近任务**看不到**它、BLE 不掉；引导页按一次返回同样两行。**只见 `返回键未接管: ...` 一行** = 回调进了但判据不成立（该行会列出是哪一项）；**一行都没有** = 回调根本没进（挂点问题）。关掉设置页开关后按返回应正常退出（该行显示 `开关=0`）（取代第 39 条）。
- **49. 已知行为变化（设计结果，非缺陷）** — 设置界面 → 引导页 的「正常返回」被吃掉（改为收后台），用户无法再用返回退回引导页；重开 app 会回到设置界面。
- **50. 操作记录改为与「配置·曲线页诊断信息」完全同款**（自适应高、无内部滚动、卡内边距挂法一致）— 两条要真机看：① **展开后页面被拉长**——正文上限 8000 字符按估算约 190 行 / ~3000dp（5~6 屏），是否可接受；若失控，压小上限或改成只留最近 N 条。② **手动刷新的补间会把日志卡的长高也一起动画**（补间范围是整页根），观感可能变成「日志卡缓慢伸长」；若突兀，最小改法是把补间范围收窄到只包状态卡（取代第 14 条，并与第 13 条的补间一并看）。
- **51. 长文本（最长 8000 字符）在无限高 + `textIsSelectable` 的正文里**，长按选择的手感与选择手柄行为未验。
- **52. `dimens_status.xml` 的 `diag_body_max_height` 已删除**（不再有「两张卡封同一个人为上限」这件事）— 通过 = 两张卡各自自适应、互不牵连。

## 间距与交互（第四轮补丁轮二）

- **53. 曲线图例把「冷端℃」「CPU℃」钉到第二行**（实现是「冷端之前强制换行」，不保证行号恒为 2：极窄屏下第一行那 4 项自己就换行时，冷端会落到第 3 行）— 通过 = 宽屏窄屏下这两条都同处一行、不与电池/制冷/风扇/热端混排；行距仍是 0dp。另记一处已知小账：`FlowWrapLayout` 的换行标记用强引用持有子视图（与既有 `fullLine` 同），当前每页只构建一次图例、不累积。
- **54. 曲线高度调节条上下留白相等**（上 = 画布下内边距 4dp，下 = 拖柄带 8dp 减去横条 4dp）— 代价是拖柄触控带由 12dp 缩到 8dp：真机重点按几下，纵向好不好按（横向长度未变）；不好按就回调该 `dimen`（调大即重新破坏上下相等）。
- **55. 状态页按钮区留白收紧** — 可见空白四处（用户给定）：**部署状态文本 → 进度条 = 0dp**、**进度条 → 按钮行1 = 5dp**、**行1→行2 = 3dp**（margin −5dp）、**行2→行3 = 0dp**（margin −8dp，**已由第 69 条③取代原 2dp/−6dp**）；行间两处各开一个 dimen（`status_button_row_gap` / 新增 `status_button_last_gap`），都是负 margin 抵掉按钮自带的 8dp 内缩，按钮外观与可点区都不变。另记：0dp 那处的「可见 0dp」不等于色带紧贴字底（TextView 行盒含字体 descent 的空白），要更贴只能上负值。通过 = 按钮区比上一版**明显**更紧凑、节奏一致；按钮外观与可点手感无任何变化。**边界口径**：可见空白 = margin + 两侧内缩 8dp（可见 3dp ↔ margin −5dp、0dp ↔ −8dp，均在安全区，后者圆角块刚好相接）。负 margin 非官方支持用法——若某些 ROM 上表现为行间塌陷，把这一个 dimen 改回正数即可，不会崩。
- **56. 参数输入框竖直内边距 8/4dp → 5/3dp** — 正文行盒中心由「比输入框几何中心低 2dp」变为低 1dp。通过 = 正文与参数名盒基本齐平；偏上偏下都改 `config_field_pad_top`。**其几何数字随后被第 58 条的框高改动重算（内容区 28dp），以第 58 条为准。**
- **57. 滚动条拖动在惯性滑动进行中不再失效（已修）** — 根因（每帧位置回写覆盖同帧拖动量）状态入 TECH_DEBT；验法 = 甩一下让页面还在滑，立刻按住滚动条往下拖，内容应**立刻**跟手；顺带复验拖动时不误触发左右翻页、松手后滑块位置与内容一致。若某些 ROM 上仍不跟手，回到本类找那个零速度 fling 的中止点。
- **58. 输入框框高 `field_height` 49dp → 36dp**（配置页各输入框与日志搜索框共用同一值、仍然等高；比日志框压缩前的原 37dp 只少 1dp）— 两条真机重点：① **浮起说明（hint 浮到框顶那条边上）与正文只剩约 1.6dp 余量**，看是否相碰或被上层容器裁掉，紧了就把 `field_height` 回调（只此一个值）；② 框高已低于 Material 的 48dp 触控建议，点按手感是否偏小。竖直内边距仍是 5/3dp（第 56 条），36dp 下的几何：内容区 [5,33] 共 28dp、行盒中心 19dp（比框中心低 1dp）。
- **59. 路径输入框「切页回来内容不显示」已修**（根因：开了 `scrollHorizontally` 的输入框横向滚动上限按 `Layout.getWidth()` 恒为 1MB 计算，视窗被摆到文本右侧空白里；只有「已布局好之后再填同一个值」这条路无人纠正，切页回来正好命中）— 验法三条：① 切页回来 path 框有内容；② 失焦后滚动位置在本次启动内保持；③ 换成更短的路径后框里能看到文本、不出现空白。
- **60. 路径框显示端已裁定并实现：「始终右」**（首次进页也显示右段/文件名）— 做法 = 填值后（非聚焦时）把光标钉到末尾，使框架 pre-draw 的 `bringPointIntoView(selectionEnd)` 与本类 `applyPathScroll` 同值 → 谁后写都落右段。**由此带来的口径变化**：`PATH_SCROLL_X`（第 22 条后半句「记住本次启动内编辑时滚到的位置」）只在「记忆视窗本身已包含文本右端」时仍可见，否则会被 pre-draw 拉到右端——即被「始终右」压过；`applyPathScroll` 的 `min(记忆值, 上限)` 一字未动。若日后要恢复记忆值可见，改的是**钉光标的位置**（钉到记忆视窗内的字符处，让框架走「保持现有位置」分支），不是 `PATH_SCROLL_X`；属设计取舍，未做。真机复核四条：①首次进页 path 框显示右段；②切页回来同样显示右段、不再空框；③失焦后位置保持；④点按路径框时光标落在点按处、键盘弹出后视窗不跳到末尾。
- **61. 输入框说明的两个问题都已修**——①**显示不全**（浮起说明字号 = EditText 字号 16sp，原削宽地板只按「数字放得下」取 72dp → 说明被省略号截断）：修法 = 地板改成**每字段** `max(72dp, 该字段说明宽)`，削到地板仍放不下就换行（**该地板口径已被第 64 条取代**；本条修掉的问题本身仍有效）。②**没顶到圆角**：修法 = `gravity` 由 `center` 改 `start|center_vertical` + 新增 `textAlignment="center"`（框内数值仍居中）。**真机重点（明确取舍）**：不截断的代价是**行数变多**——「风扇转速范围」与「热端过温制冷削减」由 2 行变 3 行；若不接受这个高度，唯一替代是回到「地板不含说明」并接受截断，二选一。
- **62. 输入框竖直内边距最终定为 5/2dp** — 规律：**落点偏移只由 `padTop − padBottom` 决定**，本次差 3dp → 落点低 **1.5dp**，内容区可用高 29dp。**代价**：正文中心与参数名盒的差为 1.5dp；要严格同高需 `padTop = padBottom`（如 4/4 → 偏移 0）。最薄处 **0.75dp**（说明行盒下沿 → 正文行盒顶），行盒再高约 1.5dp（字号超约 17.3sp、或 fontScale ≳ 1.08）两行盒开始相接。**参数名与正文的那 1.5dp 差已由第 67 条②消掉**；本条其余数字仍有效。
- **63. 三处零散改动** — ①设置页「参数重置」卡移到「界面」卡**之后**并补 12dp 上间距（与页内分组卡同一个 `space_m`）；②日志页关键词搜索框上移 2dp（新开 `log_filter_gap`，**已被第 69 条②取代：现 4dp**）；③曲线页诊断信息与回落提示统一改称「**行数目标**」（机制描述处——环形覆盖保留、切片边界、日志渲染上限——仍作「上限」，刻意不动）。

---

## 间距与对齐（第四轮补丁轮三）

- **64. 参数输入框宽度改为「贴合内容」**（宽 = max(说明宽, 内容宽)，两者都已含框内左右 8dp 内边距）——删掉 `config_field_min_width`(72dp) 下限、`config_field_max_width`(240dp) 上限与 `CONTENT_WIDTH_TAIL`（"aa" 两字符余量），说明一侧另加 1dp 安全量（新 dimen `config_hint_slack`）；不再按可用宽削峰，只剩一道安全钳制 `min(宽, 首行可用宽)`。**第 61 条的"地板"口径自此作废**：不再有地板。真机三个重点：①说明很短的键框会明显比上一版窄；②输入时敲到比当前值更长时框内横向滚动、失焦回填后才重算宽度（框会跳一下）；③单字段说明若长于「行宽 − 参数名 − 8dp」仍会被截断（刻意留的兜底，现网最长说明约 140dp 未触及）。通过 = 短内容键不再有大片右侧空白、长说明键说明完整。
- **65. 数字框整组靠右**（`FlowWrapLayout` 新增 `setRowAlign(ROW_ALIGN_END)`，只在配置页控制区开启，容器缺省仍是靠左 → 曲线页图例/重置栏/分组卡不受影响）——行尾不再留白，参数名与框之间出现留白。**连带效果**：控制区里没有数字框的行（`APP_LAUNCH_SCREEN_GATE` 的两个开关）也整体靠右，位移 ≤ 9dp；若不想要，把 `setRowAlign` 只设在含数值字段的键上即可（一行）。通过 = 每行最右一个框的右沿贴住卡片内容右边界。
- **66. 换行后的行可用整行宽**（`FlowWrapLayout` 新增 `setFirstRowIndent`）——控制区内容盒由「参数名右沿 + 8dp 起」改成覆盖整行（`layout_marginStart = −参数名宽` + 控制区宽写死为行宽），首行再加缩进 = 参数名宽 + 8dp，故**首行落点与改动前逐像素一致**，第二行起从行首起排（算术见 `ConfigKeyRow#syncControlGeometry`）。风险边界是「首行缩进抵消负 margin」；参数名盒实测宽读到 0 时整个同步跳过、保持旧几何。通过 = 多字段键换行后那一行明显更宽（说明不再被挤到截断），且首行位置与上一版一致。
- **67. 参数名竖直对齐（本轮真 bug）**——①**开关行**：MaterialSwitch 的 M3 最小高是 48dp（不是 36dp），参数名盒却固定 36dp 且贴顶 → 参数名中心比开关中心高 **6dp**；修法 = 参数名盒高改成与对齐控件同高（整键 switch 取开关实测高；控制区第一个字段是布尔开关的键取该字段根实测高），盒仍贴顶。②**输入框行**：参数名盒中心比输入框**视觉中心**高（M3 浮起说明骑在框顶那条边上，把框的视觉中心压低）；修法 = 下移 **3dp**（新 dimen `config_label_ink_shift`，用 `setTranslationY` 实现，纯渲染位移、不改行高、不参与测量）。**开关行不叠加这 3dp**（盒高改完已正好落在开关中心；要连开关行也下移说一声）。通过 = 两类行的参数名分别与输入框框体正中、开关中心齐平。
- **68. 多值键的开关在换行时独占一行**（`FlowWrapLayout` 新增 `setAloneInRow`：之前必换行、其后也必换行，但不铺满；判定 = 首行缩进 + Σ字段宽 + 间距 > 控制区内容宽，与容器换行判据同口径）——`PID_TARGET_DIR` / `PID_SPEED_RECALL` 按新宽度是否判为换行需真机确认；`APP_LAUNCH_SCREEN_GATE`（两个开关字段）应保持并排。通过 = 判为换行的键里开关单独占一行、不与数字框同排。
- **69. 三处零散改动** — ①设置页「参数重置」卡删除说明行（连 `config_reset_hint` 字符串一并删；标题到按钮组只剩 8dp 一段间距）；②日志页关键词搜索框再上移 2dp（`log_filter_gap` 6dp → **4dp**，**取代第 63 条②**）；③状态页按钮「行2 → 行3」可见间距 2dp → **0dp**（`status_button_last_gap` −6dp → **−8dp**，**取代第 55 条**该处取值）。



# 需真机确认（2026-09-21 第二轮改动）

> **归口**：同上一节——本节只写要验什么；缺陷状态与根因见 [`TECH_DEBT.md`](TECH_DEBT.md)。
> 本节为上一轮（第二轮）改动的全部留痕：**已被第四轮取代的条目就地压成一行**（保留取代关系与裁定结论），**未被取代的仍是待验动作、逐条保留**。被本轮取代的第二轮项（输入框宽度规则、文字右对齐、拖柄手感、图例两层间距）已移除。
> 本机无 Android 运行环境，CI 只证明"能编译"。以下只有源码级依据，从未在设备上跑过。

## 参数区（配置页 + 设置页）

- **（原）输入框宽度把「内容说明」单独算进去** — **已被第四轮第 19~21、23 条取代**：数值框不再冲出窗口右边界，长说明改挂行级不再撑高字段块，同键多字段的并排改由「从最宽的框开始削、必要时流式换行」决定。
- **（原）参数内容改横向居中** — **已被第四轮第 21 条取代**：判据改由框宽计算保证（`gravity=center` 未改）。
- **左右内边距压到 8dp（原 M3 默认 16dp）** — 说明的可用宽 = 框宽 − 内边距，故说明也跟着左移、可用宽变大。需确认正文与说明是否贴到圆角上（贴太紧就把 `@dimen/field_radius` 调大）。
- **（原）正文垂直位置改在「说明下沿 → 框底」之间居中** — **已被第四轮第 24 条接续、再经第 44 条取代**：口径仍是「说明下沿 → 框底」区间，但内边距已由 12/4dp 改为 8/4dp；现状与备选见第 44 条。
- **（原）参数名改为定高 49dp 盒内居中、盒子贴顶** — **已被第四轮第 67 条取代**：参数名盒高改为与对齐控件同高、盒仍贴顶，并新增 3dp 墨迹下移（`config_label_ink_shift`）；见第 67 条。
- **参数之间间距 12dp → 6dp** — 卡内上下内边距未动。需确认展开态长列表的疏密。
- **（原）path 输入框** — **已被第四轮第 22 条取代**（新增 `scrollHorizontally`，判据收窄为「右侧内容可见 + 失焦后滚动位置在本次启动内保持」；「换短路径后有无留白」并入该条复核）；显示端另经第 59、60 条（「切页回来不显示」已修、裁定「始终右」）改动，验法以第 60 条为准。
- **设置页卡头「界面自用，守护进程不读取」徽标** — 与「未生效」同位置同样式、整组只标一次。判据是"组内有任一界面自用键"而不是"全部"：该组的 `UI_BACK_HIDE` 是要被 C 端读的，按"全部"判就一次都标不出来。需确认长徽标是否挤掉标题「界面」。
- **段标编号只在界面去掉** — 定义里仍是「[4] 界面」（`check_params.py` 断言分组标题带 [N]、`profile.conf` 段标由它派生），设置页显示时剥掉；配置页四组仍带编号。若希望定义里也不带编号，需一并改断言。

## 曲线区与外壳（第二轮原项）

- **左右内边距改为按刻度数字实测宽现算（原写死 36dp）** — 数字位数变化时绘图区会随之微移（如 3 位变 4 位），需确认抖动是否可接受（不可接受就改成按最大位数取定值）。
- **纵轴单位收进图内、与刻度数字左右对齐** — 左标题左沿 = 最左数字左沿，右标题右沿 = 最右数字右沿（后者改用右对齐绘制）。需确认两个单位的位置及是否与数字重叠。
- **（原）拖柄横条 32dp → 160dp；触控带 24dp → 12dp** — **结论仍独立成立**（横条 160dp 未变；触控带第四轮补丁轮二已由 12dp → **8dp**，见第 54 条）；横条上方留白已改（最下刻度数字 → 横条只剩 1dp）：**「按得住」与「不碰到数字」是一对相反取舍，须与第四轮第 4、5 条一起裁**。
- **拖柄只认纵向拖动** — 判据与纵向滚页面同一套（同一个 `ViewConfiguration` 的 touch slop、纵向位移占优）；定为纵向时以"那一刻"为起点，画布不会先跳。靠页面根换成 `ui/PageScrollView`：按下落在拖柄带上时该容器本次手势不接管。需实测四件事：①带上纵向拖动稳定改画布高；②带上横向滑动照旧翻页；③带外纵向拖动照旧滚页面；④带外横向照旧翻页。
- **滚动条宽度 6dp → 4dp** — 命中区仍是"滚动条宽 + 8dp 且不小于 20dp"，未变。
- **拖滚动条时禁父容器拦截** — 命中窄带按下即禁、抬起恢复。需确认按住滚动条纵向拖动不再翻页，且不命中的触摸行为未变。
- **（原）图例到档位行、图例到画布均 2dp；图例内部两行之间 6dp → 3dp** — **已被第四轮第 1 条取代**：本轮改为让「图例内部两行」与「图例→画布」向「图例→时间选择栏」看齐，验法与卡点（`minHeight` 24dp 只是下限）见该条。
- **（原）底栏高度 80dp → 53dp、条目上下内边距 12/16 → 8/11dp** — **已被第四轮第 8 条取代**：本轮把下内边距再收到 3dp（文字下移 8dp），改的是同一个卡点，结论以该条为准。
- **下侧沉浸式（系统手势条适配，本轮改动面最大的一处）** — 两个 Activity 都改走 `ui/EdgeToEdge`。需实测：①顶部标题是否仍不被状态栏压住；②底栏是否一路铺到屏幕底、手势条那条无灰带；③打字时页面是否仍被键盘顶起（配置页底部输入框、日志搜索框）；④设置页底部有无异常留白；⑤手势条是否仍可见（深色）且不与底栏抢手势；⑥三页滚动区底部有无被系统条盖住的内容。其中③（键盘）以第四轮第 47 条为准（底栏 inset 冲突已修）。


## 配置页（其余未变项）

- **分组卡头锁 48dp 后开关是否被裁** — 卡头改为定高 48dp（与不带开关的诊断卡头一致）；开关墨迹含按压状态层若超出会被裁，需目视确认（必要时改成锁开关自身高度）。
- **每参数独占一行的实际密度** — 43 键全展开后列表明显变长（原先短键会并排）；确认是否需要保留"短键并排"的例外。
- **展开三角已改为矢量图标** — 原 36sp 字符「▸/▾」在 24dp 卡头里顶点被裁；现为矢量实心三角（收起指向右、展开指向下）。配置页分组卡头、配置页诊断卡头、状态页「操作记录」三处同一套，需确认观感与方向。
- **负维度 `switch_trim_end = -6dp`** — 回收开关缩到 3/4 后左右各约 6dp 的空档。负维度在 aapt2 应属合法但未编译验证；若报 "invalid dimension"，换成字面量 `-6dp`。
- **开关缩放 0.75 的贴齐量** — 按 M3 track 52×32dp 推算两侧各内缩约 6dp；真机若不同，行尾会差几 dp（不影响功能）。
- **bool 子字段的占位标签已隐藏** — 定义里几个键的布尔子字段 label 字面就是「开关」，界面不再重复显示；有意义的 label（如「Dozing 算亮屏」）保留。
- **bool 字段「字段名 + 开关」换行后的观感** — 该行是流式容器、`minHeight=49dp`（单行与同键输入框等高），换行后由内容长高（`FlowWrapLayout` 已支持 minHeight）。需确认换行时与同键输入框的对齐、以及是否仍显拥挤。

## 外壳与曲线区（其余未变项）

- **跟手滑动翻页（ViewPager2）** — add/hide/show 与手写转场已整套替换：需确认跟手度、与页面内竖向滚动/滚动条拖动的冲突、以及屏幕边缘的系统返回手势是否抢手。
- **页面生命周期改由可见性广播驱动（本轮风险最高的一处）** — 非当前页被压到 STARTED、不派发 onPause，故日志页的「键盘不顶起」与刷新、曲线区刷新都改挂 `PageAware.onPageVisible`。须实测：切页 + 弹键盘、切页后日志是否仍在后台刷。
- **画布高度不记忆** — 同一次运行内切走再切回会保留拖动结果，重启 App 回默认 204dp（不落盘）。若要求"每次切回都回默认"，需另加一行重置。
- **底栏与 4 个新图标** — 图标是自绘矢量（info/tune/list/settings），需确认语义辨识度、选中态、以及「配置 · 曲线」这个较长标签是否被截断。
- **（原）标题栏几何** — **已被第四轮第 7 条取代**：所述 7/16dp 是第四轮前的值，现为上 4dp / 下 8dp；标题栏总高与「设置按钮压到 28dp（小于 48dp 触控规范）」的取舍以该条为准。
- **画布底色由纯白改为卡片色** — 网格色随之改用分隔线色；深色主题下的对比度需确认。

## 状态页与日志页

- **部署状态恢复逐项展示** — 哈希一致时合成一行（`apk = 设备 = <md5>`），不一致时两行供逐位比对；su 不通时逐项也照列（原因在提示行）。需确认无 su 时的观感是否误导。
- **部署后不再显示部署动作日志** — 状态区只刷探测结果，操作细节仍在「诊断信息」里。
- **三页第一个窗口顶到顶栏** — 页面上内边距归零，卡片上圆角直接贴标题栏下边界；需确认观感。**结论仍有效，但间距已变**：标题栏下留白由第四轮第 7 条从 16dp 收到 8dp，须与第 7 条一并目视。
- **（原）日志页两卡合并** — **已被第四轮第 26 条取代**：关键词过滤移入「日志区」卡内（不再单独一张卡，标题也去掉），位置与判据见该条。
- **旧配置行的行为变化** — 设备上既有的 `APP_LAUNCH_SCREEN_GATE=1 1 0` 按新两值口径取前两个 token → Dozing 变为「算亮屏」（AOD 期间也会拉起 app）。属已知取舍，未做兼容；界面与守护进程读数一致。

## 跨进程（沿用）

- **`/data/local/tmp/tempctrl_uiprefs`** — 守护进程写、宿主进程里的钩子读。钩子已在该目录读写 status 文件（属既有通道），但"钩子读"是新方向，SELinux 下能否读到需实测（读不到时按默认开启处理）。**第四轮未改此项**，但它已升格为第 41 条的「次因排查第一站」；同源状态见 [`TECH_DEBT.md`](TECH_DEBT.md) §3.3。
- **（原）`UI_BACK_HIDE` 关掉后是否真的恢复系统默认退出** — **已被第四轮第 48 条取代**（第 39 条是同一功能的旧口径）。

## 沿用（上一轮提出，本轮仍未变）

- **「窗口不被输入法顶起」用的是 Activity 级窗口模式** — 日志页可见时把窗口设为 `SOFT_INPUT_ADJUST_NOTHING`，离开/暂停/销毁三处还原。它是**窗口级共享状态**，还原失效会波及配置页输入框（键盘遮挡）。须实测：切页 + 弹键盘（本轮入口改为可见性广播，风险面比原先大）。
- **日志统计行的分子口径** — 取"文件大小"（非实际读取字节数），故文件远超尾读上限时会显示成 `已读取 2000 / 2000 行，2050.8 / 400 k`。
- **`TECH_DEBT.md` §3 的 5 类未验证项**（service.d 冷启动、root 往返、SELinux 标签、私有目录创建、参数校验盲区）仍未逐条过；同节的第 6 类（PID 调参项的稳态振荡，同源 `逻辑说明.md` §待测试项）同样未过。

---

# 约定

- **源码行号引用一律用「函数名 + 关键判据」定位**，不写 `tempctrl.c` 的纯行号——行号随编辑漂移，且本工程索引行号恒比工作树小 1。本轮已按此规则改写 `ui/LogClassifier.java` 的约 20 处行号引用。
- **`build.yml` 与目录名耦合**：重命名 `lsp模块/` 必须同步改 workflow（触发正则、哈希、编译、注入、`working-directory` 各处）。已在 workflow 文件头写明。
- **生成物落盘并纳入版本控制**（`params.json` / `profile.conf` / `逻辑说明.md` 参数表段 / `params_generated.h`）：CI 以「重跑生成 + 比对」和「三源对账」把守，判据是 `python 参数定义/check_params.py` 退出码为 0。
- **GitNexus 索引重建**：本轮目录搬迁后索引仍指向 `magisk模块(智能温控)/tempctrl.c`（已删除的路径）。在重建前，对 `tempctrl.c` 跑 `impact` / `context` / `detect_changes` 会命中死路径或返回空**且不报错**。重建：`node .gitnexus/run.cjs analyze --skip-agents-md --force`。

---

# 未完成：门禁覆盖缺口（2026-09-20 独立核验实测确认）

> 门1 / 门3 / 门4 已于 2026-09-20 闭合（结论见下表）。仅余门2。

| # | 缺口 | 现状 | 状态 |
|---|---|---|---|
| **门1** | C 端核对是「有则查、无则跳过」 | **已闭合（2026-09-20）**：判据落在**定义是否具备数值范围**（路径键无数值语义、0/1 布尔域无越界可能 → 豁免；其余"声明了却找不到 clamp"→ **ERROR**）。表体改由生成头承接后，范围从产物 `params_generated.h` 读（`parse_generated_ranges`）。实测：摘掉 `pid_ki_up_coef` 的 clamp → `[FAIL] 范围无法核对 PID_KI_RATE[1]`，EXIT=2；还原后 EXIT=0 | **已闭合** |
| **门2** | `PID_KI_DYN_T` / `GATE` / `WIN` 共 7 个取值位零覆盖 | 已改：这 7 位现在**每次运行都具名列出**（INFO「C 端无对应变量、未核对」），不再隐形。**但核对本身仍未做**——它们是「全或无提交」的跨字段护栏（`t1>t2`、`m<t1`），`VAR = clamp(...)` 正则从原理上表达不了；且 def 的 `audit.cVars` 是**显式写 null**，与门1 的"声明了却找不到"性质不同 | **部分**：豁免已显式化，自动化核对待决策（新规则 or 接受豁免） |
| **门3** | `逻辑说明.md` 是生成产物，却被 CI 变更检测的 `*.md` 排除 | **已闭合（2026-09-20）**：`detect-changes` 先把 `逻辑说明.md` 单独留下，再做 `*.md` 排除 | **已闭合** |
| **门4** | 行格式的**实参个数**无人核对（只对了转换符个数） | **已闭合（2026-09-20）**：新增断言，解析使用 `WEBUI_ROW_FMT` 的 `fprintf` 调用并按顶层逗号数实参，须等于 `WEBUI_DATA_COLS`。实测三者均为 8 | **已闭合** |

另：`参数定义/gen_params.py` 的 `SOURCES`（三源）与 `PRODUCTS`（四产物）**重叠**——`profile.conf` 与 `逻辑说明.md` 既是"来源"又是产物，它们与定义的一致性实际由断言 A（有没有忘记重新生成）保证；真正独立的对账只剩 C 一处。措辞上"三源"高估了独立性。**接入生成头后，C 端的键表与范围也改由产物承接**，这一格独立性进一步下降（换来的是一处手写、五处生成）。
