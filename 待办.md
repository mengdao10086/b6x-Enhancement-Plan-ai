## 待办

> **规则**：已实现的任务直接删除（无论是否已验证），待办内不保留「✅ 已完成」条目，也不保留供复查的草案；同一时间只保留「未实现」项。

- **电池温度停滞判断（新想法，未做）** — 电池温度停滞后视情况结合电池电流/CPU温度等相关数据进行判断
- **WebUI 曲线着色改造（延迟，仅记录，勿执行）** — 曲线**不加断点**；颜色变化用**渐变**（非硬分段）；颜色**按各序列自身特性决定**，**弃用现有固定温度/颜色映射表**。设计细节与场景 UI 借鉴（B 整档刻度 / A 面积填充已单独立项）分离。

### 动态 KI 机制（设计已定，尚未实现；以下为画不进流程图的部分）

> 流程图见 [magisk模块(智能温控)/动态KI机制流程图.md](magisk模块(智能温控)/动态KI机制流程图.md)。以下每条均为**未解决问题，或已拍板决定的落地约束与接受的风险**，不是改造方案；凡标"无实测支撑"/"无法复现"的数字，都不得当作已确认值使用。

- **T2 端量化散布远大于 T1 端** — 问题：整数码下 T2 对应点（**按默认档** T2 = 1.20，即峰值 0.20 ℃、半幅仅 2 码）的 P+Q 散布是 −7.7% ~ +27.2%，T1 端（**按默认档** T1 = 4.70）是 −10.2% ~ +14.4%，阈值附近实际触发时机比设计值漂得多。阻塞：散布源于整数量化，数学上无法消除，只能真机确认是否可接受（我独立复现时扫周期 + 相位得到 −19% ~ +36%，比该区间更宽，说明结果对"扫哪套周期/相位集合"的隐含假设很敏感）。需要：真机 P+Q 分布数据 + 明确"散布按哪套扫描集合衡量"的口径。
- **T2 配置值的下界未定（原"θ 的下界"已断链）** — 问题：T2 现为独立配置项（默认 1.20 码²），不再随 θ 变化，原推理链（θ 缩小 → T2(θ) 逼近量化地板）已不成立。新的下界对象是 T2 配置值本身：须明显高于"两码对跳"的量化地板 0.375 码²（地板的含义与"借用"关系见 M 的护栏条），当前默认 1.20 相对地板余量 3.2 倍，充足。θ 降级为纯门控半宽，只剩 [3, 10] 码（= 0.3 ~ 1.0 ℃）的边界选择问题——θ 越小门控越窄、机制可用率越低（见门控可用率风险条）。阻塞：地板由整数量化决定，是硬限制；且 0.375 是 P+Q 的地板，不是 T2 可直接借用的下限，借用关系必须先在文档里写明才能当下界用。需要：先定义"塌"的判据（T2 逼近地板后，削减量被削减下限吞掉、机制名存实亡），据此给出 T2 配置下界；θ 的边界问题并入门控可用率实测。
- **波形敏感性的口径未固定** — 问题：基准式 P+Q = 19a²/108 + a/4 是三角波专属，且它对幅度**非齐次**（含一次项 a/4），所以"某波形是三角波的 K 倍"没有唯一值，K 随基准幅度 a 变。原稿的"方波 2.35~2.64×、正弦 1.32~1.38×、锯齿 0.96~0.98×、均匀噪声 0.95~0.97×"与"方波把静默点从 0.9 ℃ 拉到 0.52 ℃"**均无法复现**（下面两个口径的静默点数字均按默认档 T1 = 4.70）：按 ±峰值二电平模型独立复算得方波 4.4~5.5×、静默点 0.90 ℃ → 0.36 ℃；改用"同一 T1 下的等效峰值之比"口径才得 2.51~2.74，与原值量级吻合，说明原值隐含了未写出的归一化口径。其中"均匀噪声 0.95~0.97×"独立模拟为 0.62~1.46，**属不可复现**，不得与已复现的方波数字并列成同等可信度。阻塞：真机电池温波不保证是三角波，波形判据在运行期拿不到。需要：固定一套口径——以三角波 P+Q = 19a²/108 + a/4 为基准，其他波形**并列报两个口径**（同 a 的 P+Q 倍数、同 T1 的等效峰值比）并注明 a 的取值；或给波形无关的判据。
- **门控可用率风险（原"耦合方向是反的"已随 T1/T2 独立而消失）** — 问题：T1/T2 不再由 θ 派生，"放宽门控会让 T1 跟着涨"的前提不成立；但门控 abs(R − B) ≤ θ（θ ∈ [3, 10] 码，即 0.3 ~ 1.0 ℃）仍是单一阈值硬开关——真实工作点偏离基准超过 θ 时机制整体关闭、削减恒为 0，为覆盖"基准下方 0.33~1.0 ℃"就必须把 θ 放到接近 10 码。而这条覆盖需求本身也是**无实测支撑**的隐含假设：0.33~1.0 ℃ 实际隐含 cpu − batt ∈ [20, 40] ℃（因为 cpu_comp = (cpu_filtered − batt − 10.0)/30，见 1885 行），**源码里一个字都没写**。阻塞：门控无滞回（抖动交后级 EMA 吸收），可用率无法从代码推导。需要：真机实测稳态实际 R − B 分布，以及 cpu − batt 的实际区间（用于替换 20~40 ℃ 这个未写出的假设），据此定 θ。
- **配置面方向反转（原"配置项 3→1"的卖点已作废）** — 问题：配置面方向与原来相反——本机制在既有 PID_KI_RATE / PID_CH_THRESHOLD / PID_SPEED 之外**再加 7 项**（T1、T2、M、θ、削减下限、N、α）。好的一面是每项直接可控、无派生链（改 θ 不再连带 T1/T2），代价是标定面从"少而耦合"变成"多而独立"。阻塞：无——这是取舍的结果，不是缺陷。需要：确认接受 7 维配置面；若要收敛，另立"组合预设"维度，不改各项语义。
- **标定策略：M 是拍板的约定值，T1 不标定** — 问题：方向机制改绝对式后，原归一化参考量已彻底出局。M = 0.75 码² 是新常量、**无推导依据**：推荐范围 0.05 ≲ M ≤ T2 = 1.20（下沿是本机制还区分得动 P−Q 的粗略量级；上沿只是"在深削减区内也能完全恢复"的**充分条件，不是硬上界**——**硬上界是 T1 = 4.70**，见 M 的护栏条），**区间内取值属拍板，不做真机标定**（M = 0.75 同时落在 ≤ T2 与 < T1 内，不受此修正影响）。T1 = 4.70 码²（峰值 0.45 ℃ / 峰峰值 0.9 ℃）同样不由 θ 推导、不做真机标定，直接用配置默认——"T1 由公式推导而来"的遗留问题随之结案。原设想的"新增日志字段 R / P+Q / P−Q + 1 秒一行 WebUI 曲线数据文件作数据源"**已作废**：该文件是滚动 720 行、断联即停写，且不含这些量，改为 pid debug 日志短时诊断（见诊断通道条）。阻塞：M 的取值无法从现有数据反推，长期数据通道被放弃。需要：接受"M 属拍板值、真机不可验证"这一取舍；T1 不做真机标定，其静默点可能偏高的风险另列为接受项（见接受风险条）。
- **符号错配（既有代码的坑）** — 问题：选升/降速率的依据是 integrand = ch − pid_target_f（1961-1962 行）的**符号**，不是 ch 的符号（源码 172/174 行注释原文即"被积项 ch − target_f"，逻辑说明.md:229 同）。错配在降温瞬态必然发生，以 integrand = 0 为界重算：sc = pid_speed_coef/10 = 10.0（1939 行），error = 0.2、cpu_comp = 1.0 时令 ch = 0 得 v = −0.12；去噪规则见 1934-1936 行（|v| ≤ 0.1 归零、超出部分向零收缩 0.1），反推原始温度变化需 −0.22 ℃/周期，×12 ≈ 2.6 ℃/分；再代入 pid_target_f ≈ error × pid_target_coef/1000 = 0.004（1944-1956 行，默认系数 20）后，分界移到 v ≈ −0.1196，偏移量级为 0.02 × error（本例 0.3%），**不改变"降温瞬态走降速率"的定性结论**，但 target_f 与 error 同号、偏移方向随 error 符号翻转，**逐周期方向未定**。所以"正侧偏差大就恢复升速"在降温瞬态里恢复的可能是**当前没在用**的那一侧。阻塞：这是既有链路的结构问题，本机制只是继承它；错配在真机瞬态下是更严重还是更轻，**待实测确认**。需要：先做真机瞬态确认，再决定本机制是否要在升降速率之外单独乘算。
- **推入口径与漏采（P1(b)）** — 问题：推入沿用既有 batt_window_changed（**含 mtime**：255-257 行，值或 mtime 任一变化即置位）。在 mtime 每周期 tick 的内核上，控制周期内几乎总有变化 → 窗口退化为时间窗口，**"温度越稳 → P+Q 越小 → 削减越狠"成为设计意图**（本次接受下来的条件性行为翻转）；在 mtime 恒定的内核上退化为纯值比较（256 行注释），温度不更新即不推样本，窗口冻结在旧样本上（不补零、不补等值），冻结窗口在长稳态下持续参与求值。漏采共**四处**，不是三处：三处 continue 是 2591「**全部设备断联**」、2603「**无可用设备**」、2627「**App/BLE 失活**」（注意真正的设备切换分支 2592-2601 **并不** continue，旧稿"设备切换"的命名不准），第四处是 pid_cycle 开头的 if (batt_raw < 0) return;（2292 行）。附带一条：短路①（2339-2342）与短路②（2346-2353，阈值 PID_CH_THRESHOLD 默认 2 = 0.2 ℃，189 行）都以 batt_window_changed == 0 为前提，故 mtime tick 内核上两者都打不到、每周期都会求值；纯值比较内核上稳态周期既不推样本也不求值，机制靠 EMA 保持上一档。阻塞：属设计固有语义（推入口径是拍板选择），改动即改行为。需要：真机确认两点——① 目标内核 mtime 是否每周期 tick（决定"越稳越狠"是否真的生效）；② 冻结窗口（纯值比较内核）在长稳态下的实际表现是否可接受。
- **样本数下限 m ≥ 6 的处理** — 问题：窗口样本数 m = count 不足 6 时机制**不干预**（两侧 scale = 1.0，即经 EMA 平滑回"不削减"），启动初期、长断连复位后（见复位路径条）、切模式后窗口为空都会命中这一支。攒够 6 个样本的墙钟时长不固定：在 mtime 每周期 tick 的内核上约 6 个控制周期 ≈ 30 s，在纯值比较内核上要等温度真的变化 6 次，可能长得多（见推入口径与漏采条）。阻塞：无（设计固有）。`m < 6` 的周期现在会打 `count_low` 原因行，与"正常不削减"可区分（见诊断通道条）。需要：确认真机冷启动/重连后这段时间不削减是否可接受。
- **历史方差门控的可复用骨架** — 问题：本模块曾有方差门控（pid_var_push / pid_var_compute），在"单累积器重构"里被删（CHANGELOG v2.6 记为"移除…方差门控…等复杂补丁"）；其逻辑与本次**方向相反**（"稳定 → 放开 I 累积" vs 本次"稳定 → 收紧 KI 速度"），且是整数二值门。阻塞：只可复用"环形缓冲 + 缺档线性插值"骨架，数学部分不同，不能照搬。需要：确认是否要沿用其插值方案（本设计的窗口刻意不插值，两者前提冲突）。
- **诊断通道与其硬限制** — 问题：体检数据不新增内存表（用户明确选择**不做**内存二维表），改为增记进 pid debug 日志，共 **8 个字段**：R、P+Q、P−Q、g_up、g_down、scale_升、scale_降、r。退化周期（`count_low` / `guard_fail` / `gate_off`）不打印这 8 个字段，改打一行原因文本（如 `count_low m=3/6`、`guard_fail M>=T1`、`gate_off |R-B|=8 > θ=5`）；只有正常周期才打 8 字段。`batt_window_changed=0` 的周期（含 snap 周期）两种行都不出。但 pid_log（1983 行）被 debug_mode && debug_pid 双开关门控（356-359 行），写进 LOG_MAX = 16256 字节的滚动日志（147 / 825 行），**只够分钟级短时诊断，不是长期标定通道**。阻塞：通道容量是硬限制，放弃内存表后没有替代的长期记录。需要：明确接受"**M 选得对不对长期无法验证**"这一取舍；若日后要长期验证须另立通道，且不得复用 WebUI 曲线文件（滚动 720 行、断联即停写，2441 行）。
- **复位路径必须覆盖新状态** — 问题：pid_reset_core（2146-2163 行）在启动（2524 行）与长断连（2258 行）两处被调用，reconnect_align（2237-2266 行）另清 batt_changed_since_ctrl / batt_window_changed（2239-2240 行）。新机制的全部状态（窗口、r、g_up/g_down、scale、EMA s）若不一并清零，切模式/重连后会带着断连前的窗口与削减档继续跑。阻塞：无（纯实现约束）。需要：实现时把新状态挂进 pid_reset_core，并确认 reconnect_align 的清窗口动作与之一致；可对照 pid_ki 的全文件写点（1963 累积、1973/1974 钳位、2147 复位，仅此四处）。
- **dt 口径要全文统一** — 问题：dt = (now − pid_last_change_time)/5.0 钳 0.6~6（2356-2359 行），而 pid_ki 的累积**刻意不乘 dt**（172 / 1959 行）。因此"标称升速率 0.020"是**每重算周期**、不是每秒；且该周期墙钟 ≥ 5 s 且可变（主循环 time(NULL) - last_ctrl >= 5 配 sleep(1)，2564 / 2650 行）。阻塞：口径问题，非缺陷。需要：文档与配置说明一律写"每重算周期"，不得写成"/秒"。
- **KI 速率与抖动度量跑在两条序列上** — 问题：pid_compute 收到的 batt_10 是**滤波值** pid_batt_filtered（2372 行），而窗口 P+Q 与门控 abs(R − B) 用的是 **batt_raw**。"改 KI 速率"与"衡量抖动"不同源。阻塞：序列不同源，求值点无法做一致性检查。需要：确认该错配在真机瞬态（filter_lag 为真，2325 行）下的表现是否可接受；符号错配那条只讨论了符号，未涉及此处。
- **EMA α = 0.3 无真机依据（约定值）** — 问题：α = 0.3 **属约定值**（已拍板采用），但它**无任何真机依据**。按 s += α × (target − s)、每 5 s 步进一次复算：时间常数 τ ≈ 5/0.3 ≈ **16.7 s**（一阶近似；按离散解 τ = −5/ln(1−α) 更精确为 ≈ 14.0 s）；旧稿写的"≈ 50 s"是 **≈ 3τ 的"接近收敛"时间**（约 95%），不是 τ 本身，两者不是一回事。又因步进周期墙钟 ≥ 5 s 且可变（见 dt 口径条），τ 也随之浮动。阻塞：与 M 相同，长期数据通道被放弃，真机数据无法用于反推该取值。需要：按约定值对待——已拍板采用、取值无真机依据，真机数据可用于验证、必要时再调；与 M、θ 同列，本次接受"暂用 0.3"。
- **M 的护栏与"借用"说明** — 问题：M 的**硬上界是 T1，不是 T2**——削减区内 |P−Q| ≤ P+Q < T1，故 |P−Q| < T1 恒成立；若 M ≥ T1，则 max(P−Q, 0)/M < 1 恒成立、g **永远到不了 0**，该侧**永无完全恢复**，**这才是真正的失效边界**（护栏要拦的是 M ≥ T1）。M ≤ T2 = 1.20 只是"在深削减区内也能完全恢复"的**充分条件**，不是硬上界；M > T2（且 M < T1）**不是失效**——完全恢复被挤到中幅区（P+Q ≥ M 时才可能 g = 0），而那里 r 小、恢复量的绝对值也小，方向机制**被削弱但仍工作**，顶多给"效果打折"提示。反例（原稿"P+Q ≤ T2 是 M > T2 的推论"不成立）：M = 2.0、P+Q = 3.0、P−Q = 2.2 → max(P−Q, 0)/M = 1.1 被钳到 1 → g_up = 0（升侧完全不削减），降侧 g_down = 1 → scale = 1 − r = 1 − 0.243 = 0.757（r 按默认档 T1/T2 算得 0.243），**方向机制正常工作**。另一条硬护栏：**M ≤ 0 必须硬拒绝**——**M == 0 恒产 NaN，与 P−Q 是否为 0 无关**：无分支写法两侧都算，max(·, 0) 必有一侧为 0（P ≠ Q 时是少数侧，P == Q 时两侧都是），0/0 = NaN → 顺 g → scale 污染该侧，且该侧 EMA 一旦存入 NaN 就**永久是 NaN**（s += α(target − s)，NaN 无出口）→ 等 integrand 符号轮到该侧即经 ki_rate 污染 pid_ki（P == Q 时两侧同时中招、立即生效），**只有复位能救**；所以硬拒绝不是保险，是必须。M < 0 不产 NaN，但 max(P−Q, 0)/M ≤ 0 恒被钳到 0 → **g ≡ 1** → 两侧都按满额 r 削减（scale = 1 − r），**方向区分彻底消失**（不是不削减，是退化成只削减、不辨方向）。**必须写明"0.375 是借用、不是推导"**：量化地板 0.375 码² 是 **P+Q** 的地板（"两码对跳"即 ±0.5 码，d = ±0.5 配 pow_k(d) = d(d+1)/2 → 0.375），**不是 |P−Q| 的地板**；拿它当 M 下沿的锚点只是借"最小可辨别量级"的含义，否则后人会以为 M 是从地板推出来的。阻塞：无。需要：实现时落地两条校验（M ≤ 0 硬拒绝、M ≥ T1 拦截），M > T2 只给提示；并在配置注释里写清借用关系与"真失效边界是 T1"。
- **配置项全开与硬护栏** — 问题：在既有 PID_KI_RATE、PID_CH_THRESHOLD、PID_SPEED 之外新增 7 项——T1、T2、M、θ（门控半宽，[3, 10] 码）、削减下限（scale 的 0.5 地板）、N（窗口长度，默认 36）、α（EMA，默认 0.3），全部走 parse_pid_cfg 的 clamp 校验风格（547-548 行即 clamp(?, 1, 1000)）。硬护栏：**0 < M < T1**、T1 > T2、α ∈ (0, 1]、削减下限 ∈ (0, 1]、N ≥ 下限；**M > T2 只给"效果打折"提示、不拦**（T2 不是 M 的硬上界，见 M 的护栏条）。阻塞：**配错没有运行时兜底，会直接污染积分**——T2 ≥ T1 → r < 0 或 0/0 产 NaN；**NaN 会穿透钳位**（1973-1974 行的 pid_ki > budget / pid_ki < 0 对 NaN 均为假，1979-1981 行的 out 钳位同理），M == 0 恒产 NaN（两侧都算，至少一侧 0/0）；M ≥ T1 → 削减区内 g > 0 恒成立、该侧永无完全恢复（真失效，但不报错）；M < 0 → g ≡ 1、方向区分消失（也不报错）。且现有 clamp 是 int 版（880-884 行），浮点路径不能直接复用。需要：五项校验在读配置时**拒绝**非法值（不是仅钳位），并确认 NaN 的唯一拦截点仍是 pid_reset_core。
- **方向机制的结构性弱点** — 问题：因 |P−Q| ≤ P+Q，**在机制动作最强的小摆动区，|P−Q| 天生就小 → g 趋近 1 → 方向机制恰恰在最需要它的地方最不灵**；深削减区（P+Q < T2）还常被削减下限（0.5）吞掉。M 没有中间解：调小（M → 0+）会把量化噪声当成偏向——任何微小的 P−Q > 0 都使比值越过 1、g = 0，该侧被噪声直接放到"完全不削减"；调大（M ≥ T1）则 g > 0 恒成立，该侧**永无完全恢复**。具体例（两例偏向程度相同）：P+Q = 0.5 码²、P−Q = 0.45 码² → g_up = 1 − 0.45/0.75 = 0.40，仍要压 40%；P+Q = 4.0 码²、P−Q = 3.6 码² → 3.6/0.75 = 4.8 被钳到 1 → g_up = 0，完全放过。阻塞：这是绝对形式（max(P−Q, 0)/M 无归一化）无法绕开的硬限制。需要：接受该弱点；若要改进需另立维度（例如按 P+Q 归一化的备选形式），本次不改。
- **本次接受的两条风险** — 问题：① T1 = 4.70 来自三角波假设，**真机波形更尖则实际静默点低于 0.9 ℃**——这是本次接受下来的主要风险；② P1(b)：mtime tick 的内核上"温度越稳 → 削减越狠"成为设计意图（见推入口径与漏采条）。阻塞：两条都由拍板决定，代码不提供保证。需要：真机验证波形与实际静默点（对应①），确认真机内核 mtime 行为（对应②）。
