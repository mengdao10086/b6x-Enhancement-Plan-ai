# 待办

> **归口（2026-09-22 固化）**：本文件只记**待做事项（动作）**——要做什么、真机上要看什么判据。**已知缺陷及其状态与根因**归 [`TECH_DEBT.md`](TECH_DEBT.md)；同一事项两边都有时，TECH_DEBT 留状态与根因，本文件只留待做动作并链接过去，不在此另写一份状态。
> **规则**：已实现的任务不保留叙述——只留一行「裁定结论 + 日期 + 链接（TECH_DEBT / CHANGELOG / 对齐报告）」；同一时间只保留「未实现」项，不保留供复查的草案。
> **规则（2026-09-22 固化，针对累积待确认区）**：**完成的内容仅删除，不增加待确认内容。** ——改动做完即从本文件删去，不再另建「需真机确认」这类累积区；依据与过程属 git 历史。

# 约定

- **源码行号引用一律用「函数名 + 关键判据」定位**，不写 `tempctrl.c` 的纯行号——行号随编辑漂移，且本工程索引行号恒比工作树小 1。本轮已按此规则改写 `ui/LogClassifier.java` 的约 20 处行号引用。
- **`build.yml` 与目录名耦合**：重命名 `lsp模块/` 必须同步改 workflow（触发正则、哈希、编译、注入、`working-directory` 各处）。已在 workflow 文件头写明。
- **生成物落盘并纳入版本控制**（`params.json` / `profile.conf` / `逻辑说明.md` 参数表段 / `params_generated.h`）：CI 以「重跑生成 + 比对」和「三源对账」把守，判据是 `python 参数定义/check_params.py` 退出码为 0。
- **GitNexus 索引重建**：本轮目录搬迁后索引仍指向 `magisk模块(智能温控)/tempctrl.c`（已删除的路径）。在重建前，对 `tempctrl.c` 跑 `impact` / `context` / `detect_changes` 会命中死路径或返回空**且不报错**。重建：`node .gitnexus/run.cjs analyze --skip-agents-md --force`。

---

## 待做

- **电池温度停滞判断（新想法，未做）** — 电池温度停滞后视情况结合电池电流/CPU温度等相关数据进行判断
- **曲线着色改造（延迟，仅记录，勿执行）** — 曲线**不加断点**；颜色变化用**渐变**（非硬分段）；颜色**按各序列自身特性决定**，**弃用现有固定温度/颜色映射表**。设计细节与场景 UI 借鉴（B 整档刻度 / A 面积填充已单独立项）分离。

---

# 待定：自适应 pid 速率（设计已定稿，仅剩变换公式待实测）

- 想法1：对比制冷强度与温度的方差（经变换处理后），制冷大于温度则降低倍率，小于则提高，提高/降低两侧可独立开关，制冷为 1 和上限时的部分不计入；以达到根据温度波动动态调节累计速度，以平衡温度控制效果与制冷强度波动，或者侧重其中一方的目的。
- 想法2：根据制冷强度值动态降低倍率；以达到更高制冷强度的值更难到达，且变化更平滑的目的。

> **状态**：可行性分析已完成（只读，未动任何代码）→ **设计已定稿（2026-09-23 拍板）**，除想法1 的**变换公式**（需先做量级实测）外无未定项；仍未动任何代码。
> **复核（2026-09-22）**：逐条对 `tempctrl.c` / `params.def.json` 核验，约 22/30 条成立；下列「现状基线」的冷值链、门控/预算两条、（想法1）量纲与自激环、（取样三条路）、实现落点三处已按代码修正。

**「倍率」的口径**：指 `PID_KDP` 与 `PID_KI_RATE`（升/降）。**想法1 改现有动态 KI（对比结果只乘 KI），想法2 是新增（KDP 与 KI 升/降三处都乘）。**

## 现状基线（分析确证）

- **`scale_up`/`scale_down` 只乘 KI 速率，完全不碰 KDP**（`pid_compute` 里 `pid_kdp` 用 `pid_kdp_coef` 原值）。所以"倍率作用于 KDP"是**新增作用点**，要改 `pid_kdp` 计算式，不是在现有基础上改。
- **时间语义不同**：`pid_kdp` 是每周期重算的瞬时量、不累积（只在温度变化时更新）；`pid_ki` 是累积量、不乘 dt（单位是"每重算周期"）。乘 KDP 只改本周期幅度，乘 KI 会改整个积分历史。
- **现有窗口只存原始电池温度一条序列**（0.1°C 码），且只在"温度窗口变化"时推入；**制冷序列从未出现过**。想法1 要新增第二条序列。
- **制冷是 PID 的输出**，从 `pid_out` 到下发过 4 道作用于**冷值**的加工：目标映射 `pid_align_cold` → 制冷限速 `rate_limit_cold` → 过温钳制（`hot_derate` 削减 `active_cold_eff_max`）→ 死区/极值去重 `should_skip_dispatch`。有效上限 `active_cold_eff_max` **每周期随 `hot_derate` 重算**。
  - **风扇转速另走一条独立链**（独立风扇计算 → `rpm_from_cold_exp` → `rate_limit_fan` 限速），不作用于冷值本身。
  - **关键推论**：进 `apply_gear_direct` 的冷值是**限速后的 `actual_cold`**，不是 `pid_align_cold`；「在 1 或上限」的判定须以 `actual_cold`／设备回传 `cooler_cold_real` 为口径。
- **动态 KI 有门控**：`ki_scale_up`/`ki_scale_down` 只在本周期**温度窗口变化**时被覆盖，否则原样保持 1.0（`pid_compute` 的 `batt_window_changed` 入参同此）——因 CPU 补偿变化、滤波 snap 触发的重算周期**削减不生效**。新机制必须显式决定是否继承该门控。
  - **制冷序列的时间口径与温度序列不一致**：温度窗口只在"温度变化"时推进，而制冷几乎每周期都在动。若照搬同一门控，两边"N 对应多少时间"不同，"两边方差直接比大小"在时间对齐上也不成立。
- **KDP 与积分预算耦合**：`pid_compute` 里 `budget = 1.0 - pid_kdp; if (pid_ki > budget) pid_ki = budget;`（抗 windup）。所以"倍率作用于 KDP"不只改本周期幅度，还会**反噬积分**（kdp↑ → budget↓ → `pid_ki` 被截断）。

## 想法1：结论、硬伤与取样三条路

**扩展点现成**：升降两侧独立作用已是现状（`g_up`/`g_down` + `scale_up`/`scale_down` 按 `integrand` 符号分乘），是扩展不是冲突；还可顺带补上现有机制**没有总开关**的缺口。

**三处硬伤**：

1. **量纲** — 温度方差单位"码²"（默认锚点 T1 = 4.70 码²；「≈ 峰峰值 0.9 ℃」的换算只写在 `逻辑说明.md` 的阈值来源节，**代码里没有该公式**，且同节自述**无真机标定**——别把 4.70 当已标定锚点）。制冷方差单位"制冷²"：取值域默认 `PID_COLD_RANGE = 1..190`（配置可放宽：下限 0 / B6X 上限 194 / B7X 上限 255），但「典型量级数百至数千」**库内零依据**（只有本节这么写），而它正是下面这条结论的唯一地基——需实测标定，否则本项降级为待验证。直接比大小几乎**恒有"制冷 > 温度"** → 判据退化为恒真 → **永远降倍率**，与"动态调节"的意图相反。这不是调参问题，是会让功能失效的错误。原文"经变换处理后"未定义，**最阻塞的一项**。
2. **自激环** — 用控制器**输出的抖动**调**自己的增益**，构成最短闭环。现有动态 KI 用**输入侧**（温度）抖动，中间隔着被控对象，稳定性本质更好。振荡表现为中低频极限环，周期与窗口跨度同量级（N 默认 36 记的是**温度变化次数**、不是时间，窗口只在温度变化时推进，所以 36×5 s = 180 s 是**最快填满**的下限估法，实际跨度通常更长），且易被积分预算钳制与制冷限速掩盖成"偶发过冲"，不好定位。
3. **贴边排除可能把机制吃空** — 稳态时输出常逼近上限（高制冷）或下限（低负载），排除饱和区后样本可能长期少于门槛 6（窗口 N 默认 36）→ 机制整体退化为"不干预"。判定必须用**每周期重算的 `active_cold_eff_max`**（含 `hot_derate` 实时削减），不能用配置上限或 194/190 字面量。

**取样三条路**：目标制冷 `pid_align_cold`（限速**前**的目标值，闭环最短、自激最硬）／C 端实际 `actual_cold`（被限速削峰，系统性低估真实抖动）／设备回传 `COLD_REAL`（C 侧变量 `cooler_cold_real`；物理最真，但有已知不可信窗口：−1 由 `>= COLD_MIN` 滤掉，**占位值 1 不能靠值域过滤**（`1 ≥ COLD_MIN`），真实就绪条件是同帧 `run_mode_seen`）。

## 想法2：结论与三处耦合（方向冲突已裁定为"不处理、并存"）

- **与 `COLD_RPM_MAP` 不重复** — 一个调风扇转速（映射层）、一个调 PID 增益（控制层），可并存。但两者目的表述文字上很像，定义里要写明分工。
- **与 `RATE_LIMIT_COLD` 方向正相反** — 现在升速步长 = `基础值 + (电池温度 − 基准) × 倍率/10`，即**越热升得越快**；想法2 要让高制冷难到达。两级串联叠加后可能表现成"温度压不住、制冷又爬不上来"。
- **与 `HOT_DERATE` 有分母耦合** — 若用 `active_cold_eff_max` 做归一化分母，过温削减期分母变小 → 归一化值更高 → 降得更狠，等于过温保护期二次压制。
- **化解路径（仅备查）** — 若"高制冷"明确限定在**贴近饱和区**那段（已满负荷，再快无意义、平缓反而减少设备侧过冲），则与现有设计不冲突且与"饱和区排除"自洽。**裁定（2026-09-23）：不做区段限定、两者并存，本条非待做项。**

## 设计定义（2026-09-23 拍板）

### 共同

- **取样量两侧有意不同**：想法1 用限速后真正下发的 `actual_cold`（对应"制冷强度波动"）；想法2 用**上次 PID 算出的目标值** `pid_align_cold`（更平滑）。
- **新增配置键（暂定清单）**：倍率上界（默认 2）、想法1 升/降两侧开关、样本不足复位窗口数 M、想法2 曲线 6 参数（输入轴与输出轴各自的 上界/下界/拐点）、想法2 三处变换倍率（KDP / KI 升 / KI 降）。**总开关不新增**。

### 想法1

| 项 | 裁定 |
|---|---|
| 结构 | 温度项（现有评估器）+ 制冷项（同一评估器算制冷序列）→ **对比**（制冷项 > 温度项则降倍率、反之提高）→ 结果**仅乘 KI 速率**；**KDP 不参与**，并**取代**现有"温度项单独乘 KI"的做法 |
| 升降两侧 | 各有一次对比、各一个结果；**关掉某侧 = 该侧完全回到基础值**（含现有温度项） |
| 总开关 | **不新增**——两侧开关共同担任该职能（两侧全关＝机制停） |
| 倍率区间 | [下界, 上界]：下界沿用 `PID_KI_DYN_GATE` 第二值（默认 0.50）；**上界新增，默认 2** |
| 评估节拍 | 制冷项**不继承**温度那个门控，按自己的节拍每周期评估；温度项门控原样不动 |
| 「在 1 或上限」判定 | 判**限速后的 `actual_cold`**；上限用运行时 `active_cold_eff_max`（含过温削减）、下限用 `pid_cold_min`；**含等号、裕量 0**（`near_extreme` 已用 2×死区定义"接近极值"，不再引入第三个数字）；命中的样本**不计入对比**（按原始想法） |
| 样本不足 | 沿用现有 `count_low`（不干预、保留旧 scale）+ **连续 M 个窗口不足则缓步复位到 1.0**（M 取 3~5） |
| 真机观测点 | 撞 `budget = 1 − pid_kdp` 抗 windup 钳制的次数——上界 2 倍会在部分工况被预算截断 |

> 表中「评估节拍」与末两行未经你表态，按 2026-09-22 的建议写入；不同意直接说。

### 想法2

| 项 | 裁定 |
|---|---|
| 取样 | `pid_align_cold`（上次 PID 算出的目标值） |
| 分母 / 过温期 | 分母用**含过温削减的** `active_cold_eff_max`；**过温期不暂停** → 过温期降得更狠（**有意为之**，作为额外保守）。真机若出现"过温期制冷爬不上来"，调节点是拐点/上下界与三处变换倍率 |
| 曲线 | **输入轴（制冷）与输出轴（削减量）各自可配 上界/下界/拐点** → 两段线性 + 两端平台（6 参数）。`floor` 已由"输出轴下界"吸收，不另设 |
| 作用点 | **KDP、KI 升、KI 降 三处都乘**，且**三处各配一个变换倍率** |
| 方向冲突 | **不处理，两者并存**（2026-09-23 裁定：实际不冲突） |
| 与想法1 重叠 | 两者都乘 KI，但都是正标量乘法、**数值上可交换**（无需约定先后）→ 只需在日志里**分开记两个因子**，否则出问题无法归因 |

## 仍未定（1 项）

- **想法1 的「经变换处理后」公式（归一化分母）** — 死锁在**量级实测**上：先加一段**只读观测**（不改控制行为：把温度与制冷两个抖动强度每窗口记进 status 或日志），跑真机几段典型工况拿量级，再按「同一评估器 + 各自除以本侧容许尺度（温度侧用 T1）」定公式。**未实测前不要写死任何系数**——「制冷方差典型量级」目前库内零依据。

## 实现落点与次序

- **落点**：`ki_dyn_push` / `ki_dyn_eval` / `ki_dyn_ema` / `ki_dyn_reset` / `ki_dyn_log` 五函数 + **`ki_dyn_powk`**（易漏：`ki_dyn_eval` 算偏差和用的三角数变换，若同一变换要用到制冷方差，改的就是它）+ `pid_compute` + `pid_cycle` + `parse_pid_cfg`/`ki_dyn_cfg` + `pid_reset_core`。
- **次序**：R1 已落地，原先「先冻结 def 格式」的前置**已解除**——新键是在 `params.def.json` 里加**一条多字段对象**（`cVar`/`cTable`/`rangeMacro`/`docOrder`/`audit` 等），`profile.conf`／`逻辑说明.md` 参数表／C 端边界宏与键表由 `gen_params.py` 生成、`check_params.py` 在 CI 里对账。**但生成器只出范围宏，C 端 `clamp` 必须手写**（门1 判据：声明了范围却找不到 clamp → ERROR），所以落点是 def + `tempctrl.c` 两处，不是"加一行"。
- 与 R2/R3/R4/R7 均无关（都已落地），都不用等。**注意**：曲线第 8 列写的就是 `actual_cold`，而读端 `COL_COUNT = 8` 只消费 0~6、**[7] 有意不消费** → 要把新量（制冷方差、当前 scale）画进曲线可**先复用这一列，不必改列数**；真要加列才改行格式，届时写端 `WEBUI_ROW_FMT`／`WEBUI_DATA_COLS` 的 CI 两条断言（转换符个数、实参个数）与读端 `COL_*`／`COL_COUNT`、第 8 列去向说明都要同步。
- 新增状态必须挂进 `pid_reset_core`（现有 `ki_dyn_reset()` 就挂在它末尾），否则切模式/重连后会带着旧窗口与旧削减档继续跑。
- 若新增配置键，`params.def.json` 的 min/max 与 C 端 clamp 的一致性、以及 `逻辑说明.md` 参数表的同步，均已由生成器与审计覆盖，无需手工核对。
- **新增键逐个都要 def 条目 + C 端 `clamp`**（见上条）。另外：想法1 的两侧开关会让现有动态 KI **首次可被关闭**（现状只能把 T1 调极大使其不触发），实现时 `ki_scale_up`/`ki_scale_down` 的产生处要能把该侧直接输出为 1.0。

---

# 仅记录：已定案 / 不建议动（无待做动作）

> 下列内容均已定案，或属设计约束与历史记录：**没有待做动作，后续也不建议改动**。除非对应机制被重新引入，否则不要动这里的东西。本块只作记录，不产生工作项。

## R16：跨文件重复信息重构（余 1 项）

> R3 / R7 已于 2026-09-20 落地（C 端 status 字段常量表 `STF_*` + `status_field_value()`；曲线读端列常量与第 8 列去向说明）。落地明细见 [参数定义/对齐报告.md](参数定义/对齐报告.md) 与 [CHANGELOG.md](CHANGELOG.md)。

| # | 重复的信息 | 现状 | 收敛到 | 备注 |
|---|---|---|---|---|
| **R16** | 已删除机制的「墓碑」条目 | `逻辑说明.md` 内 15 处声明 Gear / `PID_OUT_FILTER` / `BOOT_AT` / `CTRL_MODE` / `LOG_TRIM_LINES` / `PID_GAIN` / `webroot` 等已删 | 无（保留） | 低优先；任一机制被重新引入需同时改这些句子 |

## 命名残留

- **`CFG_WEBUI_KEY_COUNT` 名字已过时** — 五个界面专用键已由 `WEBUI_*` 改名为 `UI_*`，但生成头里这个"仅界面读取键的个数"宏仍叫 `WEBUI_`。它只是计数、无键名语义，改名要动 `gen_params.py` 的写出处；属遗留命名，不影响功能。
- **`参数定义/对齐报告.md` 与 `.claude/` 下的历史笔记里仍有 `WEBUI_*` 键名** — 属**当时事实的记录**，不改写历史。

## 门禁覆盖缺口（2026-09-20 独立核验实测确认）

> 门1 / 门3 / 门4 已于 2026-09-20 闭合；门2 已于 2026-09-22 裁定接受豁免（理由落在 `params.def.json` 的 `audit.description`，核对改由 C 端边界测试承担）。**本节全部已定案，无待做动作。**

| # | 缺口 | 现状 | 状态 |
|---|---|---|---|
| **门1** | C 端核对是「有则查、无则跳过」 | **已闭合（2026-09-20）**：判据落在**定义是否具备数值范围**（路径键无数值语义、0/1 布尔域无越界可能 → 豁免；其余"声明了却找不到 clamp"→ **ERROR**）。表体改由生成头承接后，范围从产物 `params_generated.h` 读（`parse_generated_ranges`）。实测：摘掉 `pid_ki_up_coef` 的 clamp → `[FAIL] 范围无法核对 PID_KI_RATE[1]`，EXIT=2；还原后 EXIT=0 | **已闭合** |
| **门2** | `PID_KI_DYN_T` / `GATE` / `WIN` 共 7 个取值位零覆盖 | 已改：这 7 位现在**每次运行都具名列出**（INFO「C 端无对应变量、未核对」），不再隐形。**但核对本身仍未做**——它们是「全或无提交」的跨字段护栏（`t1>t2`、`m<t1`），`VAR = clamp(...)` 正则从原理上表达不了；且 def 的 `audit.cVars` 是**显式写 null**，与门1 的"声明了却找不到"性质不同 | **已裁定（2026-09-22）：接受豁免**——不写正则核对（正则从原理上表达不了跨字段护栏），豁免理由落在 `params.def.json` 的 `audit.description`，核对改由 C 端边界测试承担 |
| **门3** | `逻辑说明.md` 是生成产物，却被 CI 变更检测的 `*.md` 排除 | **已闭合（2026-09-20）**：`detect-changes` 先把 `逻辑说明.md` 单独留下，再做 `*.md` 排除 | **已闭合** |
| **门4** | 行格式的**实参个数**无人核对（只对了转换符个数） | **已闭合（2026-09-20）**：新增断言，解析使用 `WEBUI_ROW_FMT` 的 `fprintf` 调用并按顶层逗号数实参，须等于 `WEBUI_DATA_COLS`。实测三者均为 8 | **已闭合** |

另：`参数定义/gen_params.py` 的 `SOURCES`（三源）与 `PRODUCTS`（四产物）**重叠**——`profile.conf` 与 `逻辑说明.md` 既是"来源"又是产物，它们与定义的一致性实际由断言 A（有没有忘记重新生成）保证；真正独立的对账只剩 C 一处。措辞上"三源"高估了独立性。**接入生成头后，C 端的键表与范围也改由产物承接**，这一格独立性进一步下降（换来的是一处手写、五处生成）。
