# 待办

> **归口（2026-09-22 固化）**：本文件只记**待做事项（动作）**——要做什么、真机上要看什么判据。**已知缺陷及其状态与根因**归 [`TECH_DEBT.md`](TECH_DEBT.md)；同一事项两边都有时，TECH_DEBT 留状态与根因，本文件只留待做动作并链接过去，不在此另写一份状态。
> **规则**：已实现的任务不保留叙述——只留一行「裁定结论 + 日期 + 链接（TECH_DEBT / CHANGELOG / 对齐报告）」；同一时间只保留「未实现」项，不保留供复查的草案。
> **规则（2026-09-22 固化，针对累积待确认区）**：**完成的内容仅删除，不增加待确认内容。** ——改动做完即从本文件删去，不再另建「需真机确认」这类累积区；依据与过程属 git 历史。

# 约定

- **源码行号引用一律用「函数名 + 关键判据」定位**，不写 `tempctrl.c` 的纯行号——行号随编辑漂移，且本工程索引行号恒比工作树小 1。本轮已按此规则改写 `ui/LogClassifier.java` 的约 20 处行号引用。
- **`build.yml` 与目录名耦合**：重命名 `lsp模块/` 必须同步改 workflow（触发正则、哈希、编译、注入、`working-directory` 各处）。已在 workflow 文件头写明。
- **生成物落盘并纳入版本控制**（`params.json` / `profile.conf` / `逻辑说明.md` 参数表段 / `params_generated.h`）：CI 以「重跑生成 + 比对」和「三源对账」把守，判据是 `python 参数定义/check_params.py` 退出码为 0。
- **GitNexus 索引重建**：目录搬迁后若索引仍指向旧路径，对该文件跑 `impact` / `context` / `detect_changes` 会命中死路径或返回空**且不报错**。重建：`node .gitnexus/run.cjs analyze --skip-agents-md --force`。

---

## 待做

- **电池温度停滞判断（新想法，未做）** — 电池温度停滞后视情况结合电池电流/CPU温度等相关数据进行判断
- **曲线着色改造（延迟，仅记录，勿执行）** — 曲线**不加断点**；颜色变化用**渐变**（非硬分段）；颜色**按各序列自身特性决定**，**弃用现有固定温度/颜色映射表**。设计细节与场景 UI 借鉴（B 整档刻度 / A 面积填充已单独立项）分离。

---

# 自适应 pid 速率（逻辑2 已实现；逻辑1 封存）

> **状态（2026-09-23）**：**逻辑2（冷值动态倍率）已实现并落地**——动态 KI 整套（`ki_dyn_*` + 3 个配置键）已删除、4 个 `PID_COLD_DYN_*` 键已加、App 侧护栏机制整段删除。落地明细见 [CHANGELOG.md](CHANGELOG.md) 与 [逻辑说明.md](lsp模块/daemon/逻辑说明.md) 的「冷值动态倍率」节；设计与过程属 git 历史，本文件不再保留其叙述。
> **本文件此后只留未实现项：逻辑1（封存）**。
> 文中代码事实于 2026-09-22 逐条对 `tempctrl.c` / `params.def.json` 核验（约 22/30 条成立），失准处已修正。
> **原始想法（两条，逻辑2 已实现，逻辑1 待定）**：① 对比制冷强度与温度的抖动，制冷大则降倍率、小则提高，升降两侧可独立开关，制冷在最值处的部分不计入；② 制冷强度值越高越难到达、变化更平滑。

## 统一抽象与映射（逻辑1 启用时沿用；负半区已由 `cold_dyn_*` 实现）

- **每个逻辑各输出一个 −1~1 的抽象值**：−1 = 最大降倍率、0 = 不干预、+1 = 最大升倍率。
- **冷值域（本机制的工作域）默认 40..190**：低于 40 或高于 190 视为"在最值处"，不参与判定。配置项 `PID_COLD_RANGE` 本身与设备行为**都不动**（现为 1..190，可放宽：下限 0 / B6X 上限 194 / B7X 上限 255）。
- **KI 作用点**：两个逻辑的抽象值（各带权重）**相加**后 clamp 到 [−1, 1]，**再统一映射**；KDP 只由逻辑2 供值。
- **映射**：`倍率 = U^(sign(s) · |s|^γ)`——U = **倍率上界**（默认 2），γ = **形状指数**（默认 1）。
  - 端点：`s = +1 → U`，`s = −1 → 1/U`（默认 2 与 0.5）。
  - **相乘对称由构造保证**：`M(s)·M(−s) = 1` 对**任意** s 成立，不只是端点。例：U=2、γ=1 时 s = ±0.516 → **1.43 / 0.70**（即「0.7×1.43≈1」那条要求）。
  - γ 的作用：**>1** 中间更平、两端更陡（小波动不干预、极端才动手）；**<1** 反之；γ=1 时在 log 尺度上是直线（即 `2^s`）。
  - **推论：上下界必须互为倒数**才能真正对称 → 只开放「倍率上界」一个键，**下界自动 = 1/上界**。
  - 抽象值的直接含义：**以 U 为底的倍率对数**（U=2 时 ±1 就是一档倍频程），日志里可直接读。
  - 最终倍率**四舍五入到小数点后三位**（0.001 分辨率）。副作用：两侧各自取整会引入 ≤0.001 量级的对称性偏差，功能上可忽略。
  - ⚠️ **分段线性全部放在抽象之前**——「冷值 → 抽象值」这一段可以是两段线性 + 两端平台；**抽象值 → 倍率一律走这个指数映射、不再分段**（分段放在抽象之后会破坏相乘对称）。
- **已实现部分**：`cold_dyn_*` 走的正是这套抽象与映射（只用负半区、只降不升）；逻辑1 启用时与它共用同一套，自己只多一个正半区与一个自写评估器。

## 逻辑1（封存：暂不实现；启用时**完全从头写**，不复用已删的 `ki_dyn_*` 任何部分）

- 两个抖动强度用**自写的同一个评估器**（同窗口、同一三角数变换）分别算温度序列与制冷序列，**对比得出抽象值**（制冷 > 温度 → 负值＝降倍率；反之正值＝提高）。温度窗口与**制冷序列都要新增**（原窗口数组已随动态 KI 删除）。
- **取样量**：制冷侧用限速后真正下发的 `actual_cold`（对应"制冷强度波动"）；不选设备回传 `COLD_REAL`——它有已知不可信窗口（−1 可靠值域滤掉，但**占位值 1 只能靠同帧 `run_mode_seen` 判断**）。
- **制冷取样跟随温度窗口**：只在**温度窗口可用（有新温度样本）时**采集当时的制冷数据，两边样本一一对应 → "N 对应同一段时间"成立，不再有独立节拍与时间对齐问题。
- **贴边样本不计入对比**：判**限速后的 `actual_cold`**；上限用运行时 `active_cold_eff_max`（含过温削减）、下限取**工作域下限**（默认 40）；**边界含等号、不设裕量参数**（`near_extreme` 已用 2×死区定义"接近极值"，不再引入第二个口径）。
- **权重**：逻辑1 只有**一个**权重，**不区分 KI 升/降**——对整个 KI 给同一个抽象值贡献。
- **两侧开关**：**倍率降低侧 / 提高侧各一个**（即抽象值的负半区、正半区），关掉某侧 = 该半区不生效（该半区取 0）；**两侧全关 = 倍率恒为 1（基础值）**——这即是总开关，不另设。
- **样本不足**：沿用"不干预、保留旧值"，并**连续"样本门槛"次窗口不足（默认 6）则缓步复位到 1.0**（**复用样本门槛这一个值**，不另设参数）。
- **启用前必须先做只读观测**：制冷侧的归一化尺度死锁在量级实测上——「制冷方差典型量级数百至数千」目前库内零依据。做法：只加观测（把两个抖动强度每窗口记进 status/日志），真机跑几段典型工况拿量级，再按「各自除以本侧容许尺度（温度侧用 T1）」定公式。注意 T1=4.70 与「≈ 峰峰值 0.9 ℃」的换算只写在 [逻辑说明.md](lsp模块/daemon/逻辑说明.md) 阈值来源节、**代码里没有该公式**且自述**无真机标定**。
- **三处已知风险**：① 量纲——不归一化就直接比大小会**恒判"制冷 > 温度"**，退化成永远降倍率；② 自激环——用输出抖动调自己的增益是最短闭环，周期与窗口跨度同量级、易被预算钳制与限速掩盖成"偶发过冲"；③ 贴边排除可能把样本吃空（靠上面的复位兜底）。
- **与逻辑2 现状的关系**：逻辑1 的正半区会**抵消**已实现的降倍率，即倍率可 > 1；届时须一并复核与 `pid_compute` 内 `budget = 1 − pid_kdp` 抗 windup 钳制的相互作用（逻辑2 只降不升，当下用不到该侧，故该冲突目前不存在）。

## 可自定义参数（仅逻辑1；均属封存）

| # | 参数 | 默认 | 说明 |
|---|---|---|---|
| 1 | 逻辑1 倍率降低侧 / 提高侧开关 | 开 / 开 | 关掉即该半区不生效（取 0）；两侧全关 = 总开关关闭 |
| 2 | 逻辑1 权重（作用于整个 KI） | 1 | 0 = 逻辑1 不生效 |
| 3 | 逻辑1 温度侧尺度 | T1 | 现成的抖动阈值 |
| 4 | 逻辑1 制冷侧尺度 | 待实测 | 启用前须先做只读观测标定 |
| 5 | 逻辑1 复位窗口数 | 复用样本门槛（6） | 连续 6 个窗口样本不足 → 缓步复位到 1.0 |
| 6 | 窗口长度 N / 样本门槛 | 36 / 6 | 原动态 KI 的窗口参数已随机制删除；启用逻辑1 时需重新加为配置键 |
| 7 | 日志开关（抽象值 + 最终倍率） | 关 | 诊断性关键，建议先开 |

**不建议开放**（开了会打架或破坏性质）：制冷序列的取样量（已定 `actual_cold`）、时间门控的继承（固定不继承）、死区/极值的判定口径（与 `should_skip_dispatch` 现有口径冲突）、映射的分段线性（破坏相乘对称）。

## 涉及文件（逻辑1 启用时）

- **C**：`lsp模块/daemon/tempctrl.c`（自写评估器、制冷序列与窗口数组、贴边判定、半区开关与权重、日志）
- **配置面**：`参数定义/params.def.json`、`参数定义/check_params.py`（`EXPECTED_KEY_COUNT`）+ 重跑生成器；**C 端 `clamp` 必须手写**（门1 缺 clamp 直接 ERROR）
- **App**：无需改代码——键元数据全部读 `params.json`，键数动态（`ConfigStore.keyCount()`），不硬编码键名
- **文档**：`逻辑说明.md`、[CHANGELOG.md](CHANGELOG.md)（记一条，**不升版本号**——用户 2026-09-23 决定）

## 风险与真机观测点（逻辑1 启用时）

- 启用前**必须先做只读观测**标定制冷侧尺度（见上）；量纲不做归一化会退化成永远降倍率。
- 正半区引入后倍率可 > 1 → 会出现"撞 `budget = 1 − pid_kdp` 钳制"的工况（真机观测点）。
- 自激环：真机看振荡周期是否与窗口跨度同量级；贴边排除是否把样本吃空（靠复位兜底）。
- 曲线第 8 列写的是 `actual_cold`，读端只消费 0~6、**[7] 有意不消费** → 要画新量可**先复用这一列、不必改列数**；真要加列才动 `WEBUI_ROW_FMT`/`WEBUI_DATA_COLS` 的 CI 两条断言与读端列常量。

---

# 仅记录：已定案 / 不建议动（无待做动作）

> 下列内容均已定案，或属设计约束与历史记录：**没有待做动作，后续也不建议改动**。除非对应机制被重新引入，否则不要动这里的东西。本块只作记录，不产生工作项。

## R16：跨文件重复信息重构（余 1 项）

> R3 / R7 已于 2026-09-20 落地（C 端 status 字段常量表 `STF_*` + `status_field_value()`；曲线读端列常量与第 8 列去向说明）。落地明细见 [参数定义/对齐报告.md](参数定义/对齐报告.md) 与 [CHANGELOG.md](CHANGELOG.md)。

| # | 重复的信息 | 现状 | 收敛到 | 备注 |
|---|---|---|---|---|
| **R16** | 已删除机制的「墓碑」条目 | `逻辑说明.md` 内多处声明 Gear / `PID_OUT_FILTER` / `BOOT_AT` / `CTRL_MODE` / `LOG_TRIM_LINES` / `PID_GAIN` / `webroot` 等已删 | 无（保留） | 低优先；任一机制被重新引入需同时改这些句子 |

## 命名残留

- **`CFG_WEBUI_KEY_COUNT` 名字已过时** — 界面专用键已由 `WEBUI_*` 改名为 `UI_*`，但生成头里这个"仅界面读取键的个数"宏仍叫 `WEBUI_`。它只是计数、无键名语义，改名要动 `gen_params.py` 的写出处；属遗留命名，不影响功能。
- **`参数定义/对齐报告.md` 与 `.claude/` 下的历史笔记里仍有 `WEBUI_*` 键名** — 属**当时事实的记录**，不改写历史。

## 门禁覆盖缺口（2026-09-20 独立核验实测确认）

> 门1 / 门3 / 门4 已于 2026-09-20 闭合；门2 已随三键删除而闭合（2026-09-23，见下）。**本节全部已定案，无待做动作。**

| # | 缺口 | 现状 | 状态 |
|---|---|---|---|
| **门1** | C 端核对是「有则查、无则跳过」 | **已闭合（2026-09-20）**：判据落在**定义是否具备数值范围**（路径键无数值语义、0/1 布尔域无越界可能 → 豁免；其余"声明了却找不到 clamp"→ **ERROR**）。表体改由生成头承接后，范围从产物 `params_generated.h` 读（`parse_generated_ranges`）。实测：摘掉 `pid_ki_up_coef` 的 clamp → `[FAIL] 范围无法核对 PID_KI_RATE[1]`，EXIT=2；还原后 EXIT=0 | **已闭合** |
| **门2** | （原）`PID_KI_DYN_T` / `GATE` / `WIN` 共 7 个取值位零覆盖 | 原为「全或无提交」的跨字段护栏（`t1>t2`、`m<t1`），`VAR = clamp(...)` 正则从原理上表达不了；2026-09-22 曾裁定接受豁免（理由落在 `params.def.json` 的 `audit.description`）。**2026-09-23 三键随动态 KI 删除 → 缺口消失**：豁免条目已从 `audit.description` 与 `audit.cVars` 移除，审计现报「无 C 变量 0」，全库已无跨字段拒绝分支 | **已闭合（2026-09-23，随键删除）** |
| **门3** | `逻辑说明.md` 是生成产物，却被 CI 变更检测的 `*.md` 排除 | **已闭合（2026-09-20）**：`detect-changes` 先把 `逻辑说明.md` 单独留下，再做 `*.md` 排除 | **已闭合** |
| **门4** | 行格式的**实参个数**无人核对（只对了转换符个数） | **已闭合（2026-09-20）**：新增断言，解析使用 `WEBUI_ROW_FMT` 的 `fprintf` 调用并按顶层逗号数实参，须等于 `WEBUI_DATA_COLS`。实测三者均为 8 | **已闭合** |

另：`参数定义/gen_params.py` 的 `SOURCES`（三源）与 `PRODUCTS`（四产物）**重叠**——`profile.conf` 与 `逻辑说明.md` 既是"来源"又是产物，它们与定义的一致性实际由断言 A（有没有忘记重新生成）保证；真正独立的对账只剩 C 一处。措辞上"三源"高估了独立性。**接入生成头后，C 端的键表与范围也改由产物承接**，这一格独立性进一步下降（换来的是一处手写、五处生成）。
