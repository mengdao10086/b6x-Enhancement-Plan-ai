又是一个究极冷饭啊
本模块依旧主要由 DeepSeek 编写，由我测试和小修改。


依旧先说不建议使用的情况：

1. 非飞智B6X散热器用户的：这个模块基于飞智B6X开发者工具（com.flydigi.waspwing.experimental）写的，不准备（也没有能力）适配其他散热器。

2. 非小米系设备的：我仅在 Redmi K60 上测试过，不过脚本用到的接口在小米系机型上这些年应该没什么变化，所有理论上大部分小米系都能用，但具体情况我也不清楚，自行实测。（不过其他品牌手机也可以自己去github下源码然后替换对应路径然后编译就是了，一共就那几个接口也没多复杂）


背景故事：
升了安卓16后发现，飞智散热器开发者工具App UI一直显示"扫描中"，但是蓝牙实际上能连上散热器，所以就写了个 LSPosed 模块修复。
修完之后寻思，反正都是改一次，不如顺带把幻想好久的基于手机温度的真-智能温控也搞进去得了，原本的基于制冷面温度的温控属于是智能了个寂寞，低负载冻手高负载没用的。于是又搞了个脚本，接管温度决策和档位控制。


两模块关系图：

飞智 App 进程                       脚本进程
┌─────────────────┐             ┌───────────────┐
│  LSPosed 模块   │ <──────────>│ magisk模块脚本 │
│  BLE 连接/断联   │  存活检测   │ 每 5 秒：      │
│                 │             │ 读温度→决策 ┐  │
│WaspWingManager ←│───指令──────│← 下发指令  <┘  │
│  .setRunMode    |am broadcast └───────────────┘
│     ↓           |
│    BLE → 散热器 |
└─────────────────┘


主要运行流程

1. 初始化
- 释放 App 进程出现（pgrep 检测飞智 App）
- 创建状态文件（/data/local/tmp/tempctrl.status），供模块写入 BLE 心跳
- 读取存档档位，无存档默认档位 5

2. 连接检测（每 5 秒）
- 三重检查：pgrep 进程存活 + 状态文件 mtime 心跳（≤16s）+ BLE=1
- 断联 → `actual_level = target_level` 对齐（清零待执行步伐）→ 等待复活
- 重连 → 强制下发当前档位

3. 逐步执行（每轮最多 ±1 档）
- 决策与执行分离：`main_loop` 只计算应该变动的挡位，而不实际下发
- 每轮连接检测后，向 `target_level` 方向变动 1 档
- 压制档位突变带来的噪音突变

4. 温度读取
- 电池温度：`/sys/class/power_supply/battery/temp`（原生 0.1°C）
- CPU 温度：`/sys/class/thermal/thermal_zone[N]/temp`，自动扫描可用 zone，一阶低通滤波（α=0.20）
- 电池电流：`/sys/class/power_supply/battery/current_now`，取绝对值，不平滑

5. 紧急干预（CPU 温度 + 电池电流双源）
- 进入（OR 逻辑）：CPU > 65/75/85°C 或 电流 > 5/6/7A → 触发 1/2/3 级紧急
- 退出（AND 逻辑）：CPU 和电流都低于恢复阈值才降级
- 紧急时强制最低档位：6/8/10 档

6. 电池温度调档
- 基准 35°C，三级调整：±0.7°C 死区不动，±2.0°C 内 ±1 档，超过 ±2.0°C ±2 档
- 趋势豁免+峰值反补：温度趋势反向时豁免，骤变时反补 1~2 档压制过冲
- 温度读数不变时跳过（防重复）
- 档位变动冷却期：每次变动后冻结 3 个周期（15 秒）

7. 配置热重载
- 所有阈值通过 `profile.conf` 配置（与二进制同目录）
- 文件 mtime 变化 → 自动重读，无需重启
- `CONFIG_ENABLED=0` 可跳过配置加载，全部使用代码默认值测试

8. 日志输出
- 日志输出至 `/cache/tempctrl.log`（默认 5KB，自动滚动）
- 包含档位变化、紧急等级、进程状态等信息
- 格式：`[15 14:30:22] 挡位5（+1）`表示现在5档，且刚才升了一档

档位对照

| 档位 | 模式 | 目标温度 | 风扇 | 制冷 | 说明 |
|-----|------|---------|------|------|------|
| 12 | 固定功率 | — | 6000 | 190 | 制冷峰值，在此之后b6x的鳍片解热能力不足，制冷量反而倒退 |
| 11 | 智能温控 | 12°C | 5800 | auto | |
| 10 | 智能温控 | 13°C | 5500 | auto | 紧急3最低 |
| 9 | 智能温控 | 14°C | 5100 | auto | |
| 8 | 智能温控 | 15°C | 4600 | auto | 紧急2最低 |
| 7 | 智能温控 | 16°C | 4000 | auto | 自此之后的智能模式温度波动会比较大 |
| 6 | 智能温控 | 18°C | 3400 | auto | 紧急1最低 |
| 5 | 固定功率 | — | 2900 | 80 | |
| 4 | 固定功率 | — | 2500 | 60 | |
| 3 | 固定功率 | — | 2200 | 40 | |
| 2 | 固定功率 | — | 2000 | 20 | |
| 1 | 固定功率 | — | 2000 | 10 | 伪待机 |

---

关于后续

和回写模块一样，感觉已经挺完善的了，以后不确定还会不会更，各位当停更了就行（又是发布即完结这一块[受虐滑稽]）。

虽然目前还留了几个能搞的方向，但优先级都不高：
- 档位表可配置化（把 4 张查表数组改成 profile.conf 配置，支持自定义档位数量和参数）
- UI 模式选择器在智能温控时闪烁，固定功率时圆点空白（不影响使用，如果碰到修改参数无效，重新选择一下散热器模式进行了）

也还是如果各位有啥好想法可以评论，我感觉有道理的话可能会更。但和本模块无关的就不要说了，不做大杂烩模块[二哈]